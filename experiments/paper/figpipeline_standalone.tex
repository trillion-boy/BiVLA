% ===========================================================================
% STANDALONE preview of the pipeline figure (v2, all six methods).
% Compile THIS FILE BY ITSELF in Overleaf to see the figure.
% For main.tex, the two lines
%     \usepackage{tikz}
%     \usetikzlibrary{positioning, arrows.meta}
% go in the PREAMBLE (above \begin{document}), and only the tikzpicture goes
% in the body.
% ===========================================================================
\documentclass[border=6pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta}
\usepackage{amsmath}

\begin{document}
\begin{tikzpicture}[
    >=Stealth, font=\small,
    box/.style={draw, rounded corners=2pt, minimum height=9mm,
                minimum width=15mm, align=center, inner sep=3pt},
    tag/.style={draw, dashed, rounded corners=2pt, align=center,
                font=\scriptsize, inner sep=3pt, text width=25mm},
    flow/.style={->, thick},
]
% --- pipeline, left to right ---
\node[box] (obs) {Observation\\$o_t$};
\node[box, right=13mm of obs] (enc) {Visual\\encoder};
\node[box, right=20mm of enc] (dec) {Decoder\\$N$ layers};
\node[box, right=13mm of dec] (head) {Action\\head};
\node[box, right=15mm of head] (env) {Environment};
\draw[flow] (obs) -- (enc);
\draw[flow] (enc) -- (dec);
\draw[flow] (dec) -- (head);
\draw[flow] (head) -- (env);
% --- feedback arrow above, carrying the horizon ---
\draw[flow] (env.north) -- ++(0,7mm)
      -| node[above, pos=0.25, font=\footnotesize]
      {re-query every $h = c\,r$ steps} (obs.north);
% --- the six methods, grouped by where each acts, with its role ---
\node[tag, below=10mm of obs]  (t1) {\textbf{Foveation} (control)};
\node[tag, below=10mm of enc]  (t2) {\textbf{VLA-Cache} (baseline)\\\textbf{Temporal fusion} (candidate)};
\node[tag, below=10mm of dec]  (t3) {\textbf{Depth pruning} (candidate)};
\node[tag, below=10mm of head] (t4) {\textbf{Action repeat} (control)\\\textbf{Guarded reuse} (candidate)};
\draw[->, dashed] (t1) -- (obs);
\draw[->, dashed] (t2) -- (enc);
\draw[->, dashed] (t3) -- (dec);
\draw[->, dashed] (t4) -- (head);
\end{tikzpicture}
\end{document}
