% ---------------------------------------------------------------------------
% Figure environments for Section IV-B. Final main-paper set of 2026-09-13
% after a 13-agent review of all 35 catalog figures against the paper text:
% four figures, in order of importance, with the fourth conditional on the
% page budget. All four are the mentor's catalog figures unchanged; panel
% identities and the SmolVLA stack note live in the captions. Paths assume
% paper_figures/pdf is uploaded next to the main file. Supplement candidates are commented
% out at the bottom.
% ---------------------------------------------------------------------------

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/success_change_bridge.pdf}
  \caption{Success change from the dense policy on SimplerEnv WidowX, in
  percentage points, for all 13 intervention settings on the six WidowX
  backbones. Panels read left to right and top to bottom as CogACT,
  CronusVLA, MiniVLA, OpenVLA, SpatialVLA and UniVLA. Colors identify
  families and match the row labels. Values are
  point estimates from the paired runs, and the paired test of
  Section~IV-A decides significance in the text.}
  \label{fig:success-widowx}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/cross_setting_consistency.pdf}
  \caption{For each setting, the number of the 13 backbone and environment
  combinations, with LIBERO suites pooled per backbone, whose success is
  lower, unchanged or higher than dense. Lower and higher count any nonzero
  change, and unchanged includes the cells of RQ4 whose gates never fired.
  The OpenVLA LIBERO conservative-fusion cell enters with its pooled
  success over the four suites, the Long suite from its run summary, and
  without a paired test. Under the paired test, six of the 285 setting and
  combination cells raise success significantly, on three combinations.}
  \label{fig:consistency}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/ablation_depth_pruning.pdf}
  \caption{Depth pruning across every backbone and environment as the
  number of removed decoder layers rises. Left, success change from dense
  in percentage points. Right, dense time per step divided by intervened
  time per step. LIBERO suites are pooled per backbone, so 13 lines stand
  for the 22 pairs of the text. Marker shape encodes the environment,
  circle for WidowX, square for Google robot and triangle for LIBERO. The
  SmolVLA speedups at budgets two and four come from a second software
  stack and are not read (Section~IV-A). Values are point estimates,
  and the paired test decides significance in the text.}
  \label{fig:ablation-depth}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/ablation_action_repeat.pdf}
  \caption{Action repeat across every backbone and environment at repeat
  factors of two and four. Left, success change from dense in percentage
  points. Right, dense time per step divided by intervened time per step.
  LIBERO suites are pooled per backbone, and marker shape encodes the
  environment as in Fig.~\ref{fig:ablation-depth}. Values are point
  estimates, and the paired test decides significance in the text.}
  \label{fig:ablation-repeat}
\end{figure*}

% ---- Supplement candidates (not in the main paper) -----------------------
% success_change_libero: same layout as Fig. 1 for OpenVLA, SmolVLA and
% UniVLA on LIBERO with suites pooled. Its render prints "Incomplete
% coverage" in the OpenVLA conservative-fusion row and cannot be
% regenerated until the per-episode file for that cell arrives; if used,
% the caption must say so.
% \begin{figure*}[t]
%   \centering
%   \includegraphics[width=\textwidth]{paper_figures/pdf/success_change_libero.pdf}
%   \caption{Success change from the dense policy on LIBERO, all four
%   suites pooled per backbone, for all 13 settings. Panels are OpenVLA,
%   SmolVLA and UniVLA. The OpenVLA conservative-fusion row is marked
%   incomplete because its Long suite was not available when the figure
%   was drawn, and the completed cell (45 of 100) is reported in
%   Section~IV-B.}
%   \label{fig:success-libero}
% \end{figure*}
%
% tradeoff_univla_libero and tradeoff_cogact_base_simplerenv_bridge: one
% backbone each at 6.5 in; every family-best point is in Table I or II and
% the informative points are in Figs. 1, 3 and 4.
