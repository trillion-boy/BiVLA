% ---------------------------------------------------------------------------
% Figure blocks for Section IV, 2026-09-15 (lab meeting): Fig. 2 is the
% six-panel trade-off row with all 14 settings, Fig. 3 the depth and repeat
% row, Fig. 4 the consistency chart. Files live in figure/ on Overleaf.
% Placement: Fig. 2 and Fig. 3 blocks before \subsection{Setup and
% Protocol}, Fig. 4 before \subsection{Results and Discussion}.
% The teaser block goes in main.tex right after \end{abstract}, so it floats
% to the top of the right column of page 1.
% ---------------------------------------------------------------------------

\begin{figure}[!t]
  \centering
  \includegraphics[width=\columnwidth]{figure/teaser_bridge_bars.pdf}
  \caption{Success of all 14 configurations on the six SimplerEnv WidowX
  backbones, the original policy counted as one of six tricks. Within a
  trick the bars run from its first to its third setting in the order of
  Section~IV-A, and the dotted line marks the original. No trick raises
  success far above the original on any backbone, and action repeat and
  depth pruning lower it far below on several.}
  \label{fig:teaser}
\end{figure}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figure/tradeoff_bridge_row.pdf}
  \caption{Every setting of every trick on the six SimplerEnv WidowX
  backbones, placed by its speedup per environment step and its success
  change from the original policy (the star). Panels read left to right as
  CogACT, CronusVLA, MiniVLA, OpenVLA, SpatialVLA and UniVLA, colors
  identify tricks, and within a trick the circle, square and triangle mark
  its first, second and third setting in the order of Section~IV-A. The
  shaded corner is faster and no worse than the original.}
  \label{fig:tradeoff-widowx}
\end{figure*}

\begin{figure*}[!t]
  \centering
  \includegraphics[width=\textwidth]{figure/ablation_depth_repeat_row.pdf}
  \caption{Depth pruning and action repeat on every backbone and
  environment. Panels read left to right as depth pruning success change,
  depth pruning speedup, action repeat success change and action repeat
  speedup, against the number of removed decoder layers or the repeat
  factor. Speedup is dense time per step divided by intervened time per
  step, and LIBERO suites are pooled per backbone, so 13 lines stand for
  the 22 pairs of the text.}
  \label{fig:ablation-depth}
\end{figure*}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figure/cross_setting_consistency_col.pdf}
  \caption{For each setting, the number of the 13 backbone and environment
  combinations of Fig.~\ref{fig:ablation-depth} whose success is lower,
  unchanged or higher than the original policy, counting any nonzero
  change.}
  \label{fig:consistency}
\end{figure}
