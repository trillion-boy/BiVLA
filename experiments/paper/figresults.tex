% ---------------------------------------------------------------------------
% Figure environments for Section IV-B. The six figures are the mentor's
% selection in plots/paper_figures/PAPER_SELECTION.md, at the full
% two-column width they were designed for. Paths assume the mentor's
% paper_figures/pdf folder is uploaded next to the main file. Drop figures
% from the bottom of this file first if the page limit bites. The seventh,
% commented out, is the significance-colored variant of cross_setting.
% ---------------------------------------------------------------------------

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/success_change_bridge.pdf}
  \caption{Success change from the dense policy on SimplerEnv WidowX, in
  percentage points, for all 13 intervention settings. Panels read left to
  right, top to bottom, as CogACT, CronusVLA, MiniVLA, OpenVLA, SpatialVLA
  and UniVLA. Colors identify families. Values are point estimates from the
  paired runs, and the paired test of Section~IV-A decides significance in
  the text.}
  \label{fig:success-widowx}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/success_change_libero.pdf}
  \caption{Success change from the dense policy on LIBERO, all four suites
  combined per backbone, for all 13 settings. Panels are OpenVLA, SmolVLA
  and UniVLA.}
  \label{fig:success-libero}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/tradeoff_univla_libero.pdf}
  \caption{Success change against effective time-per-step speedup for
  UniVLA on LIBERO, relative to the dense policy in the same environment.
  The star is the dense policy, the shaded region is faster and more
  successful, and the dashed line connects the nondominated settings
  without implying significance. The overview panel shows all settings and
  the five panels show one family each with its own axis limits.}
  \label{fig:tradeoff-univla}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/tradeoff_cogact_base_simplerenv_bridge.pdf}
  \caption{Success change against effective time-per-step speedup for
  CogACT on SimplerEnv WidowX, in the layout of
  Fig.~\ref{fig:tradeoff-univla}. Depth pruning at a budget of two is the
  one table cell in the study that is both faster and more successful.}
  \label{fig:tradeoff-cogact}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/ablation_action_repeat.pdf}
  \caption{Action repeat across every backbone and environment. Left,
  success change from dense at repeat factors of two and four. Right,
  time-per-step speedup. Lines are backbone and environment pairs.}
  \label{fig:ablation-repeat}
\end{figure*}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{paper_figures/pdf/cross_setting_consistency.pdf}
  \caption{For each setting, the number of the 13 backbone and environment
  combinations (LIBERO suites combined per backbone) whose success is
  lower, unchanged or higher than dense. Counts are point estimates.
  Under the paired test only six settings across the study raise success
  significantly, all on three combinations (Section~IV-B).}
  \label{fig:consistency}
\end{figure*}

% Optional variant colored by the paired test rather than by sign:
% \begin{figure*}[t]
%   \centering
%   \includegraphics[width=\textwidth]{paper_figures/pdf/cross_setting_significance.pdf}
%   \caption{As Fig.~\ref{fig:consistency}, with each combination colored
%   by an exact two-sided McNemar test at $p<0.05$ on episodes matched by
%   task and index. Incomplete combinations are excluded.}
%   \label{fig:consistency-sig}
% \end{figure*}
