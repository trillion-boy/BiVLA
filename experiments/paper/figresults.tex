% ---------------------------------------------------------------------------
% Figure environments for Section IV-B, combined layout of 2026-09-14 after
% the mentor's request to combine the plots. Three files in figures_combined/,
% drawn by make_combined_figures.py from the mentor's common.py with the data,
% colors and markers unchanged and only the subplot grid and size changed:
% success_change_bridge_row (1x6, 7.16x3.3 in), cross_setting_consistency_col
% (single column, 3.5x3.4 in), ablation_depth_repeat_row (1x4, 7.16x2.7 in).
% The catalog paper_figures/pdf is untouched. Overleaf paths use figure/.
% ---------------------------------------------------------------------------

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures_combined/success_change_bridge_row.pdf}
  \caption{Success change from the dense policy on SimplerEnv WidowX, in
  percentage points, for all 13 intervention settings on the six WidowX
  backbones. Panels read left to right as CogACT, CronusVLA, MiniVLA,
  OpenVLA, SpatialVLA and UniVLA. Colors identify families and match the
  row labels. Values are point estimates from the paired runs, and the
  paired test of Section~IV-A decides significance in the text.}
  \label{fig:success-widowx}
\end{figure*}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures_combined/cross_setting_consistency_col.pdf}
  \caption{For each setting, the number of the 13 backbone and environment
  combinations, with LIBERO suites pooled per backbone, whose success is
  lower, unchanged or higher than dense. Lower and higher count any nonzero
  change, and unchanged includes the cells of RQ-4 whose gates never fired.
  The OpenVLA LIBERO conservative-fusion cell enters with its pooled
  success over the four suites, the Long suite from its run summary, and
  without a paired test. Under the paired test, six of the 285 setting and
  combination cells raise success significantly, on three combinations.}
  \label{fig:consistency}
\end{figure}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures_combined/ablation_depth_repeat_row.pdf}
  \caption{Depth pruning (left pair) and action repeat (right pair) across
  every backbone and environment as the number of removed decoder layers
  or the repeat factor rises. In each pair, left is the success change from
  dense in percentage points and right is dense time per step divided by
  intervened time per step. LIBERO suites are pooled per backbone, so 13
  lines stand for the 22 pairs of the text. Marker shape encodes the
  environment, circle for WidowX, square for Google robot and triangle for
  LIBERO. Values are point estimates, and the paired test decides
  significance in the text.}
  \label{fig:ablation-depth}
\end{figure*}
