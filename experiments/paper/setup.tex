% ---------------------------------------------------------------------------
% Setup and Protocol -- ICRA form. Draft 2, 2026-08-26.
%
% This section pays the Introduction's debts. Every quantity the Introduction
% asserted and deferred is settled here with its record:
%   6,910 episodes        -> the decomposition below (paper/EpisodeCounts.md)
%   McNemar / alpha=0.05/38 -> "Pairing and significance" (verify_all.py)
%   "check determinism"   -> "Determinism" (Report_EN.md sec 3.4)
%   "four baselines above the published figures" -> "Baselines" (Report 3.8)
%   backbone specs         -> Table II (make_backbone_table.py)
%   the grid itself        -> Table I (make_grid_table.py)
%
% Numbers reproduced by experiments/verify_all.py. Style matches the
% Introduction: no em-dashes, no semicolons, no prose colons.
%
% DRAFT 2 changes (Reviewer 2 pass, 2026-08-26):
%  - dropped "each spends a different resource" and the fixed-patch-grid
%    sentence, both verbatim echoes of Related Work.
%  - dropped "a point we return to in the results" (foreshadowing).
%  - "broken-setup objection" -> plain environment check.
%  - CONTENT FIX. Draft 1 said the 11-of-96 UniVLA/Bridge baseline gap was a
%    "different GPU model" effect and that we "rebuilt the column" to avoid a
%    hardware boundary. Both are wrong. Report_EN.md 3.4.0 (primary): the two
%    baselines were re-taken on a MATCHED card, differ by 3.1 points at
%    p = 0.5488 (indistinguishable from chance), and the files record no GPU
%    at all, so 3.1 points is a BOUND on an unprovable hardware/session
%    confound, not a measured card effect. UniVLA always ran on an L4, so no
%    column ever crossed a hardware boundary to be rebuilt around. The 3.1-pt
%    bound belongs in Limitations (Hardware.md sec 8), not here; Setup states
%    only the same-environment reproduction and the pairing rule.
%
% ⚠️ EXPANSION. The mentor is running five more backbones (TurboVLA, CoTinyVLA,
% SmolVLA, FLOWER, MiniVLA) on LIBERO and SimplerEnv. When those land, the grid
% shape (3x2), the per-cell counts, the 4,464 / 6,910 totals, and the family
% size 38 all change and must be regenerated. The PROTOCOL below -- pairing,
% McNemar, the Bonferroni construction, the determinism rule, the within-cell
% argument -- does not change. Keep the protocol prose and re-derive the counts.
% ---------------------------------------------------------------------------

% NOTE 2026-08-26: this is now IV.A, a subsection of "Experiments and
% Evaluation". The interventions paragraph moved to III.B (bagoftricks.tex);
% Setup carries only the protocol. In main.tex this file follows
% \section{Experiments and Evaluation}. If compiled alone, change the
% \subsection below back to \section.

\subsection{Setup and Protocol}

\textbf{The grid.} We cross three open backbones (OpenVLA, SpatialVLA, UniVLA)
with two SimplerEnv suites, WidowX-Bridge and Google Robot/Fractal. UniVLA
releases only a Bridge checkpoint, so five of the six cells are filled and
UniVLA/Fractal is left empty (Table~\ref{tab:grid}). Each filled cell runs the
same eight conditions, a baseline and seven interventions, over a fixed episode
set, 96 episodes on Bridge (four tasks at 24) and 135 on Fractal. The grid is
therefore $3 \times 96 \times 8 + 2 \times 135 \times 8 = 4{,}464$ episodes.
Beyond it, eligibility-window controls, a foveation keep sweep, extra depth
variants, determinism re-runs, and a task-level failure probe bring the
campaign to $6{,}910$. Table~\ref{tab:backbones} lists the checkpoint, decoder
depth, actions emitted per call, and GPU for each backbone. Section~III gives
the three interventions and the conditions swept within each.

\textbf{Pairing and significance.} Every condition replays the initial states
of its cell's baseline, so each episode has a matched control. We compare a
condition against that baseline with McNemar's exact two-sided test on the
discordant pairs, the episodes whose success flipped, and report the paired
change in percentage points. Across the grid this is a family of $38$ paired
tests, the five cells times the seven interventions plus three depth variants
that some cells also ran. We control the family with a Bonferroni threshold of
$\alpha = 0.05/38 \approx 0.0013$, which eight of the $38$ clear. The tables in
Section~IV-B report each change with its exact $p$-value and mark the ones that
clear the threshold; Table~\ref{tab:grid} carries the absolute rates the
changes are taken from.

\textbf{Determinism.} With greedy decoding and a seeded environment, re-running
a condition reproduces it episode for episode, 85 of 85 and 24 of 24 on two
backbones. The result files do not record the GPU, so we pair only within a
cell and require a condition and its baseline to share a run environment. The
two Fractal backbones ran on the same card model (Table~\ref{tab:backbones}),
which places the cross-backbone contrast on Fractal on matched hardware.

\textbf{Baselines against the reported figures.} To check the environment
configuration, we compare each baseline against the figure published for the
same checkpoint. Four of the five sit above it, UniVLA/Bridge by $11.4$ points,
and the fifth, SpatialVLA/Bridge, is $4.2$ points below. Because every result is
a within-cell paired change, no claim depends on these rates, and a broken
setup would depress all five together rather than leave four high and one
marginally low.
