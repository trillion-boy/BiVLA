# Audit report, Sections II and III (branch claude/serene-davinci-sy33re, 2026-09-04)

Scope per brief.md. Every item below survived two adversarial verifiers. Confirmed findings are referenced as C0 to C98 in the order they were supplied; where several describe the same sentence they are merged into one item and the merged tags are listed. Nothing confirmed was dropped. Suggested fixes are exact replacement text in house style (no em-dashes, semicolons, prose colons or hedges). Line numbers are for the files as they stand today. RelatedWork.md must be changed in step with every relatedwork.tex edit (brief line 25-26). tabletricks.tex is generated, so every Table I edit goes into experiments/make_tricks_table.py and the table is regenerated.

Within each section, items are ordered by how much they would cost the paper.

---

## 1. Errors and contradictions in Related Work

### 1.1 Look, Focus, Act is not a VLA and Gaze-Reg does not foveate (error) [C15, C16, and the line-271 half of C54]

Quote (relatedwork.tex 271-275): "On the spatial axis, foveation descends from Schwartz's log-polar mapping~\cite{schwartz}, taken up in robot vision to cut data while preserving central resolution~\cite{traver} and brought to VLAs through human gaze, as a training-time regularizer~\cite{gazereg} or as a foveation signal~\cite{lookfocusact}."

Evidence: 56_Look_Focus_Act.txt p.3 lines 321-332 (observation is one camera image plus joint angles through a ViT and Q-Former) and p.5 lines 491-493 (ViT-B pretrained from scratch with MAE), no occurrence of "language" or "VLA" outside the reference list. Gazereg.txt p.1 lines 18-24 (KL regularizer on attention, no foveation), p.3 lines 284-287 (synthetic gaze, no eye tracking), p.17 lines 2118-2132 (foveated input to the unchanged encoder gives 78.5% against 85.9% on LIBERO-Spatial). The axis names at lines 259-263 are "When the policy runs", "What it is shown", "How much network each call uses", and "spatial axis" is never defined.

Fix (replace lines 271-275):
"On the second axis, what the policy is shown, foveation descends from Schwartz's log-polar mapping~\cite{schwartz}, taken up in robot vision to cut data while preserving central resolution~\cite{traver} and brought to learned manipulation policies through gaze, as a training-time attention regularizer in VLAs~\cite{gazereg} or as the center of a foveated tokenizer in a ViT policy~\cite{lookfocusact}."

Optional support after "not whether it runs faster." (line 279): "Gaze-Reg reports 78.5\% against 85.9\% on LIBERO-Spatial when it foveates the input of an unchanged encoder at training time~\cite{gazereg}." Mirror in RelatedWork.md line 48.

### 1.2 "base OpenVLA" names a model neither cited paper ran on LIBERO, and the VLA-Cache latency is per-call GPU time (error) [C13, C77, C89]

Quote A (relatedwork.tex 293-295): "On base OpenVLA across LIBERO it reports average success of 74.7\% against 75.0\% dense at 39\% lower latency, a speed result at nearly unchanged success."
Quote B (relatedwork.tex 331-333): "which raises base OpenVLA on LIBERO by four points at under two percent overhead, a denoising result rather than a speed one~\cite{ttfvla}."

Evidence: ttfvla.txt lines 485-493 reserve "base OpenVLA-7B model without task-specific fine-tuning" for SimplerEnv (gain 1.6 points, line 621) and use "officially released task-specific fine-tuned checkpoints" for LIBERO (gain 68.4 to 72.4, Table 1 lines 640-660, caption lines 680-681). VLACache.txt lines 619-620 (official OpenVLA LIBERO weights), Table 2 lines 647-682 (75.0 vs 74.7, 51.91 vs 31.83 ms, 38.7% lower), lines 611-615 ("CUDA latency captures actual GPU runtime"), while Table I in this paper is wall-clock per environment step (tabletricks.tex 101).

Fix A (lines 293-295): "On OpenVLA's LIBERO checkpoints it reports average success of 74.7\% against 75.0\% dense at 39\% lower GPU latency per model call, a speed result at nearly unchanged success."
Fix B (lines 331-333): "which raises OpenVLA's task-finetuned LIBERO checkpoints by four points at under two percent overhead, a denoising result rather than a speed one~\cite{ttfvla}."
Mirror in RelatedWork.md lines 67-68 and 109.

### 1.3 VLA-IAP is cited for a claim its tables contradict (error) [C9]

Quote (relatedwork.tex 348-349): "Papers that use several backbones also change the benchmark~\cite{vlacache, specprune, vlaiap}"

Evidence: VLAIAP_compressed.txt p.9 lines 473-475 (OpenVLA-OFT, DreamVLA, pi0, pi0.5 on LIBERO, VLABench, CALVIN), p.10 lines 482-492 (Table 1 caption and header, DreamVLA on LIBERO and CALVIN, pi0 on LIBERO), p.11 lines 815-816 (OpenVLA-OFT on LIBERO), p.21 lines 1526, 1564, 1602. Three backbones share LIBERO and DreamVLA runs on two benchmarks, which is the second pattern the sentence describes, not the first.

Fix (lines 348-351): "Papers that use several backbones also change the benchmark~\cite{vlacache, specprune}, and where one backbone does appear on two benchmarks the others appear on one~\cite{vlapruner, gazereg, vlaiap}, so the effect of a backbone cannot be separated from the effect of a benchmark."
Mirror in RelatedWork.md lines 125-128. Correct Survey_2026-08.md line 46 and RelatedWork_Sources.md row 10 to record that OpenVLA-OFT, DreamVLA and pi0 all run on LIBERO and DreamVLA also runs on CALVIN.

### 1.4 The VLA-Cache transfer sentence cites a method VLA-Cache never ran and keeps one of three stated reasons (error) [C3, C4, C79]

Quote (relatedwork.tex 296-299): "The same paper also reports that token pruning developed for vision-language models~\cite{fastv, sparsevlm, tome} transfers poorly to VLAs, which it attributes to those methods ignoring the temporal structure of closed-loop control~\cite{vlacache}."

Evidence: VLACache.txt lines 607-610 ("we adopt two state-of-the-art token-level acceleration techniques SparseVLM [30] and FastV [29] on OpenVLA as compared methods"), Table 2 lines 647-682 has no ToMe row, ToMe appears only at line 134 in the related-work list. ToMe.txt lines 15-18 is a ViT throughput method. VLA-Cache's own explanation, lines 767-770: "operate within a single frame and disrupt spatial fidelity ... these methods target long output sequences, whereas VLA models generate short action outputs (e.g., 7 tokens)". relatedwork.tex header lines 176-180 and RelatedWork_Sources.md line 886 wrongly record the short-output reason as "not in the paper".

Fix (lines 296-299): "The same paper also reports that two token pruning methods developed for vision-language models~\cite{fastv, sparsevlm} transfer poorly to VLAs, and attributes the loss to their working within a single frame, which disrupts spatial fidelity and ignores the temporal structure of closed-loop control, and to their targeting long output sequences where a VLA emits only a few action tokens~\cite{vlacache}."
Mirror in RelatedWork.md line 71. Correct the header note at relatedwork.tex 176-178 and RelatedWork_Sources.md row 41 to record that the short-output reason is on VLA-Cache page 8. The tome key becomes uncited, so delete the tome entry from main.bib (lines 323-330) to keep check_bib.py at an exact match.

### 1.5 The Setup handoff in the notes contradicts the body (contradiction, notes only) [C53]

Quote (RelatedWork.md 277-279): "six backbones on two SimplerEnv suites, WidowX and Fractal; matched episodes with a paired test; a dense baseline under fused attention; a preregistered gate on both speed and success"

Evidence: RelatedWork.md body lines 137-144 and relatedwork.tex 360-367 say three benchmarks including LIBERO, no backbone count, and a gate of latency or success with the other within a margin. The same stale list is in the relatedwork.tex header lines 71-81. The handoff closes "If any of these is not what Setup says, RW changes to match" (RelatedWork.md 282), so a Setup written from it pulls the body backwards. Not reviewer-visible, but it is the document the mentor writes Setup from.

Fix: replace RelatedWork.md 276-282 and relatedwork.tex 73-81 with the current commitments. "Six families pending the VLA-Cache rollout answer. Every backbone on every benchmark with a released checkpoint at the evaluated size (WidowX-Bridge, Fractal, four LIBERO suites). Matched episodes paired by seed, paired test pending per-episode files. A dense baseline under fused attention, backend per backbone to be confirmed. A candidate is positive when it lowers end-to-end latency with its own signal cost included or raises success, while the other stays within a margin of dense stated in Setup. One GPU, one run per configuration."

### 1.6 Stale introduction misreports VLA-Pruner's SimplerEnv table (error, low priority, stale file) [C10]

Quote (introduction.tex 105-109): "the two published SimplerEnv comparisons we cite print the same split without remarking on it~\cite{efficientvla, vlapruner}."

Evidence: VLAPruner.txt p.8 lines 1173-1213, Move Near then Pick Coke Can, "OpenVLA 54.0% 52.8%", "FastV 38.7% 41.9%", "VLA-Cache 41.2% 40.6%", "VLA-Pruner 52.4% 50.1%", so pick coke can leads only in the FastV row. EfficientVLA.txt lines 560-640 does show pick coke can ahead in every row. NewReadings_2026-08.md line 39 already records "Move Near breaks the pattern".

Fix: none while the file is stale. If the sentence is carried forward, cite efficientvla alone, or write "the SimplerEnv table in VLA-Pruner shows the reverse ordering for its own method~\cite{vlapruner}."

### 1.7 Provenance notes name a different Schwartz paper than the bib (contradiction, housekeeping) [C26]

Quote (RelatedWork_Sources.md 96-104): "Bibliographic entry, authoritative version. The DOI page gives a longer title than Traver's bibliography does; use the DOI page. ... Vision Research 20 (1980) 645–669"

Evidence: main.bib 272-281 and the uploaded scan (c8412005-schwartz1977spatial.pdf page 1, "Biol. Cybernetics 25, 181-194 (1977)") are the 1977 paper, matching Traver ref [10] (10rastraver.txt 1908-1910). Row 47 (line 892) records the deliberate move, but lines 52, 96-97 and 118 still point at 1980. Paper text and bib are correct.

Fix: retitle the section at line 81 "### Schwartz, superseded, see row 47", add under it "Bib now cites the 1977 Biological Cybernetics paper (vol. 25, pp. 181-194), matching the uploaded scan and Traver ref [10]. The 1980 Vision Research entry below is kept only as the source of the log(z + a) attribution via Traver ref [24].", and change line 52 from "settled from the published abstract, below" to "settled from the 1977 scan, row 47". No change to relatedwork.tex or main.bib.

---

## 2. Errors and contradictions in Methods (Section III, Table I, the Section III figure)

### 2.1 Methods describes a log-polar foveation variant that never ran, and gives rho two meanings (contradiction) [C30, C95]

Quote (bagoftricks.tex 102-105): "Fixed foveation keeps a sharp center and suppresses the periphery at a keep fraction $\rho$. The log-polar variant warps the frame about its center, keeps a fraction $\rho$ of the samples, and inverts the warp. The blur variant keeps a central disc and blends increasingly blurred copies outside it."

Evidence: tricks/src/vla_tricks/foveation.py defines only foveate_blur (line 15, docstring line 20), sharp_radius = sqrt(keep_ratio * H * W / pi) at line 37 (an area fraction); run_minivla_simpler_eval.py 41 and 351-352 and benchmark_openvla_14.py 50 and 229-231 call foveate_blur for every fixed_foveation condition; a case-insensitive grep for "polar" over the tricks export returns nothing; CSV condition names carry no variant. bagoftricks.tex 97-98 promises "Here we state only what runs."

Fix (replace lines 102-107, on the mentor confirming the blur transform also ran on the CogACT, SpatialVLA, UniVLA and CronusVLA harness):
"Fixed foveation keeps a central disc sharp, covering a fraction $\rho$ of the image area, and blends increasingly blurred copies of the frame outside it. Output resolution and visual token count are unchanged, so foveation tests perceptual simplification alone."

### 2.2 The action-chunk sentence is false for two Table I backbones (error) [C91]

Quote (bagoftricks.tex 114-115): "For a backbone that emits action chunks, one call then covers $k$ times the chunk length."

Evidence: csv_digest.md lines 5 and 8 (CogACT Fractal 44.776 calls for 44.8 steps dense, 23.5 for 46.7 under repeat 2), lines 47 and 50 (SpatialVLA 49.576 for 49.6, 25.34 for 50.4), so those harnesses execute one action per call. Only UniVLA executes the chunk (lines 131 and 134, 6.375 calls for 30.2 steps, 7.25 for 71.9); tricks/adaptive_sparse_vla/eval.py 411-416 loops env.step over env_actions. experiments/make_backbone_table.py 191-194 and 266-270 record that SpatialVLA keeps raw_actions[0].

Fix: "For a backbone whose harness executes its full action chunk, one call then covers $k$ times the chunk length."

### 2.3 The depth selector and removal mechanism are stated as one procedure but two harnesses run two (contradiction) [C31, C41, with the wording items C55, C70, C75 folded in]

Quote (bagoftricks.tex 119-126): "Calibrated depth removal scores each decoder block by ShortGPT block influence ... The selector protects the first quarter of the stack and the final block, forbids removing adjacent blocks, and fixes the removed set before testing. Removed blocks are excised and the generation cache reindexed, so this is removal rather than an identity block. The budget, how many blocks are removed, is the swept quantity."

Evidence: tricks/adaptive_sparse_vla/inference.py 87-111 (BypassDecoderLayer returns hidden_states unchanged), 1032 (layers[idx] = BypassDecoderLayer(idx)), 1044-1045 (candidates from floor(0.5 N) to N, final block not protected, eval.py 166 default 0.5), 1056-1062 (fallback fills without the gap test); tricks/src/vla_tricks/depth.py 104-117 (ceil(0.25 N), protect_last 1, strict gap, raises when short) and 179-213 (true removal, layer_idx reindexed). UniVLA rows 26; 26,30; 21,24,26,30 satisfy the stated rules in outcome, so only the mechanism sentences are contradicted. Wording: relatedwork.tex 306, 308, 316 and figpipeline.tex 27 say "layer" and "Block Influence" (ShortGPT.txt 27, 121 capitalise it), the subsection heading at line 117 is "Depth Pruning" while line 119 says "depth removal".

Fix (replace lines 119-126):
"Calibrated depth pruning scores each decoder layer by ShortGPT Block Influence, one minus the mean cosine between its input and output hidden states~\cite{shortgpt}, on a calibration split disjoint from the test episodes. The selector protects a prefix of the stack, forbids removing adjacent layers, and fixes the removed set before testing, with the protected region of each harness listed in Section~IV-A. Whether a removed layer is excised with the generation cache reindexed or bypassed as an identity layer is a property of each harness, also listed there. The budget, how many layers are removed, is the swept quantity."
The clause "forbids removing adjacent layers" stands or falls with pending item 2 (CronusVLA rows, Section 4.3). Add to the Section IV-A handoff list (bagoftricks.tex header 25-26) the two protected regions (first quarter and final block on the OpenVLA-family harness, first half on the UniVLA harness) and the two mechanisms.

### 2.4 The Section III figure uses different symbols, counts and mechanism words from Methods (contradiction) [C59]

Quote (figpipeline.tex 53-54): "One query returns a chunk of $c$ actions and the action repeat $r$ holds each for $r$ steps"

Evidence: figpipeline.tex 27 ("Decoder $N$ layers"), 37 ("$h = c\,r$"), 40 (VLA-Cache tagged), 48 ("six training-free methods"), 51-52 ("reuse visual-token computation across frames", "skip decoder layers"); bagoftricks.tex 93 ("a stack of decoder blocks"), 111 ($k$), 124-125 (removal, not identity), 146-147 ("an accuracy candidate rather than a speed one"). git log 94ca34e commits the figure to Section III.

Fix: line 27 "Decoder\\layers"; line 37 "re-query every $h = c\,k$ steps"; line 40 "\textbf{Temporal fusion} (candidate)"; line 48 "The five training-free methods"; lines 51-53 "The candidates swap stable visual tokens for the previous step's (temporal fusion), remove decoder layers (depth pruning), or reuse a whole action under stability gates (guarded reuse)."; lines 53-55 "One query returns a chunk of $c$ actions and the action repeat $k$ holds each for $k$ steps, so the horizon $h = c\,k$ sets how many environment steps pass between queries." Restore the VLA-Cache tag and "six" only if its rollout rows arrive (Section 4.1).

### 2.5 The Table I footnote claims latency falls under guarded reuse in every cell, credits rises to signals only, and misstates the pick rule (contradiction) [C27, C57, C85, C60, C61, and the caption half of C28]

Quote (tabletricks.tex 101): "Each family is shown at the setting with the highest success rate in that benchmark, named in the condition column. Latency is end-to-end wall-clock per environment step, the cost of each intervention's own signals included, so it falls under action repeat and guarded reuse as well as under depth pruning."
Quote (tabletricks.tex 22): "at the strongest setting of each intervention family."

Evidence: CogACT Fractal original 169 ms (line 71) against guarded reuse moderate 170 ms (line 75), from google_robot_fractal/summary.csv 7.57/44.776 and 7.55/44.428; guarded reuse falls in the other nine cells, action repeat and depth pruning fall in all ten. Foveation raises step time in all ten cells (up to 16%, CronusVLA 126 to 146 at lines 57-58) while relatedwork.tex 276-277 says the edit "leaves the visual token count and the model computation unchanged" and the warp is never called a signal. The pick is per backbone within a benchmark (Bridge foveation keep 20% for UniVLA at line 30, keep 50% for OpenVLA at line 44). "strongest" reads as most aggressive next to "Depth prune 1" and "Depth prune 4".

Fix (make_tricks_table.py line 163, caption): "Absolute success rate, end-to-end latency per environment step, and mean episode length, at the highest-success setting of each intervention family."
Fix (make_tricks_table.py line 173, footnote): "Each family is shown at the setting with the highest success rate for that backbone and benchmark, named in the condition column. Latency is end-to-end wall-clock per environment step with the cost of each intervention's own signals and image transforms included, so a saving from fewer calls and the cost of the gate that buys it appear in the same number. It falls under action repeat and depth pruning in every cell and under guarded reuse in nine of ten. The per-setting sweeps are in the ablation tables of Section~IV."
Then regenerate. Two further footnote sentences wait on the mentor (tie rule, Section 4.8) or are ready (MiniVLA and UniVLA Fractal note, Section 4.10; UniVLA reuse note, Section 3.12).

### 2.6 Table I names a setting Methods does not (contradiction, naming) [C29]

Quote (tabletricks.tex 34 and 76): "Temporal fusion conservative"

Evidence: bagoftricks.tex 149 names it "conservative-adaptive" and defines it by the forced keyframe; make_tricks_table.py 91 maps temporal_fusion_conservative_adaptive to the shorter label.

Fix: make_tricks_table.py line 91, label "Temporal fusion conservative-adaptive", regenerate. The table is a table*, so the label fits.

### 2.7 Header comment on fusion latency is numerically wrong (error, comment only) [C34]

Quote (bagoftricks.tex 61-62): "Latency under temporal_fusion is unchanged on every backbone (OpenVLA task-aware is +12 ms)"

Evidence: univla_simpler_widowx.csv task_aware policy_median_latency_ms 861.266 vs 714.451 (+147 ms, per step 223 vs 191); minivla_simpler_widowx.csv 110.27 vs 97.08 (+13 ms); openvla_simplerenv_fractal.csv 172.809 vs 160.072 (+12.7 ms). Motion-entropy and conservative-adaptive stay within 5% per step everywhere (worst CogACT Fractal motion-entropy +4.3%).

Fix (comment): "Latency under motion-entropy and conservative-adaptive fusion is within 5 percent of dense per step on every backbone. Task-aware costs more per call on OpenVLA (+12 ms), MiniVLA (+13 ms) and UniVLA (+147 ms)."

### 2.8 Generator docstring says per-call latency is unchanged under action repeat, false for CronusVLA (error, comment only) [C35]

Quote (make_tricks_table.py 23-26): "(policy_median_latency_ms, model time per call, is not in the table. It is unchanged under action repeat and lower under depth pruning, so Results states in prose whether a saving came from cheaper calls or fewer calls.)"

Evidence: cronusvla_simpler_widowx.csv lines 2, 5, 6 give 126.3, 162.782, 239.703 ms; cronusvla_simplerenv_fractal.csv 152.561, 223.077, 356.375 ms; on those rows the column equals k times cycle_median_latency_ms (81.45 x 2, 59.926 x 4, 111.593 x 2). Flat within 2% on the other five backbones.

Fix (docstring): "(policy_median_latency_ms, model time per call, is not in the table. It is unchanged under action repeat on five backbones and lower under depth pruning. In the CronusVLA rows it equals k times cycle_median_latency_ms, so it rises with k there. Results states in prose whether a saving came from cheaper calls or fewer calls.)"

### 2.9 Stale Setup, Introduction and backbone table contradict Table I on every count (contradiction, low priority, stale files) [C66]

Quote (setup.tex 58-59): "Section~III gives the three interventions and the conditions swept within each."

Evidence: setup.tex 48-54 (three backbones, eight conditions, 4,464 and 6,910 episodes), 63-68 (McNemar, 38 tests); introduction.tex 55-56, 94, 111-113 ("three open backbones", "$3 \times 2$ backbone-benchmark grid"); tablebackbones.tex 37 ("The three backbones"); tabletricks.tex 29-97 (six backbones, five families). No main.tex exists, so the contradiction lives in the files rather than in a build.

Fix: none until those files are rewritten to the six-backbone, five-family grid.

---

## 3. Reviewer-attackable points that prose can fix

### 3.1 "differs in the selection" overstates the fusion difference from TTF-VLA, names an optional signal as standard, and leaks a Setup value [C14, C47, C72]

Quote (relatedwork.tex 335-340): "Our fusion shares TTF-VLA's hard fusion and keyframe and differs in the selection. Patches are scored by motion, image entropy and task relevance, every flagged patch protects a one-patch ring around it, and the reusable fraction is capped. We test it on every backbone in our grid against optimized dense inference on paired episodes."

Evidence: ttfvla.txt 281-287 (mask = pixel OR attention), 321-332 (per-patch pixel difference), 393-410 and 465-476 (text-to-vision attention from the previous step), 608-610 (fusion rates 30% and 50% on VLA-Cache runs); tricks perception.py 46-58 (patch_motion), 151-152 and 168-169 (relevance applied only when supplied), vla_cache.py 13-33 (relevance = text-to-vision attention); only scripts/experiments/temporal_fusion_task_aware.py passes --fusion-collect-relevance, and summary.csv picks task-aware for three of ten fusion cells. bagoftricks.tex 141-142 says "optional text-to-vision relevance" and 143 says only "or adjacent to one"; header 40-41 hands "one-patch dilation" to Setup.

Fix (lines 335-340): "Our fusion shares TTF-VLA's hard fusion and keyframe and its two signals, pixel motion and text-to-vision attention, the second in one setting only. It adds an image-entropy term, protects a ring around every flagged patch, and caps the reusable fraction. We test it on every backbone in our grid against optimized dense inference on paired episodes."
Mirror in RelatedWork.md 112-116. "paired episodes" is pending item 6 (Section 4.6).

### 3.2 Two of the three listed differences from FlashVLA are things FlashVLA also does [C11, with the "network call" wording of C56 and the line-325 "may" of C62/C71]

Quote (relatedwork.tex 324-328): "Ours differs in where the gate sits and how far it may go. It reads subsampled pixels before any network call, at frame and patch scale, requires the last two dense actions to agree in direction and gripper state, and caps consecutive reused steps before the next dense call."

Evidence: flashvla.txt 380-382 (angle between A(s-2) and A(s-1)), 403 ("Before each inference, the Trigger Thinking module ..."), 997 and Algorithm 1 line 1017 ("if Reuse Action is True and last reuse is False"), so FlashVLA also gates on the previous two actions, decides before the call, and caps consecutive reuse at one. Our caps are 1, 1, 2 (benchmark_openvla_14.py 73-77), so the aggressive preset goes further, not less far. temporal.py 117-120 has the translation floor and gripper sign that FlashVLA lacks.

Fix (lines 324-328): "Ours differs in the signal. FlashVLA compares the token sets its previous two calls selected, whereas ours reads subsampled pixels of the current frame before any model call, at frame and patch scale, and adds a gripper-state check and a translation floor. Both require the previous two inferred actions to agree in direction and both cap consecutive reuse, FlashVLA at one step and ours at a cap swept with the preset."
Mirror in RelatedWork.md 101.

### 3.3 "negative controls" promises a failure the Fractal data do not deliver [C90]

Quote (relatedwork.tex 268): "Generic shortcuts, used as negative controls." and (bagoftricks.tex 96): "The first two are negative controls and the last three are the candidates."

Evidence: csv_digest.md Fractal repeat 2 rows, CogACT 65.6 vs 66.8 at 121 vs 169 ms, OpenVLA 40.0 vs 35.6 at 159 vs 240, SpatialVLA 59.2 vs 59.2 at 263 vs 450, CronusVLA 58.4 vs 55.6 at 112 vs 155, which clears the closing gate (relatedwork.tex 364-367) for any margin of 1.2 points or more in four of ten cells, where guarded reuse saves nothing.

Fix: relatedwork.tex 268 "Generic shortcuts, used as controls." bagoftricks.tex 96 "The first two are controls and the last three are the candidates." RelatedWork.md 40 likewise. The Fractal exception is already slated for Results (relatedwork.tex header 136-137).

### 3.4 Foveation is not temporal fusion with its signal removed [C48]

Quote (relatedwork.tex 285-289): "We run fixed foveation and fixed repeat as controls. Each acts on the same axis as one candidate but without its signal, foveation for temporal fusion and fixed repeat for guarded reuse, so the pair establishes when the candidates need their gates."

Evidence: tricks/docs/literature_review.md 39 and 60-61 and methodology.md 66-67 pair fixed repeat with guarded reuse and define foveation only as a robustness and negative control for perceptual simplification; bagoftricks.tex 106-107 and 141-147 show foveation edits pixels and reuses nothing, while fusion has a selector, not a gate. The header at relatedwork.tex 218-220 records the pairing as the author's own answer with no source.

Fix (lines 285-289): "We run fixed foveation and fixed repeat as controls. Fixed repeat is guarded reuse with its gate removed, and foveation degrades the input with no signal at all, so the pair establishes what the candidates' signals buy."
Mirror in RelatedWork.md 57-59.

### 3.5 The temporal-axis paragraph misattributes FlashVLA's decrease and SpecPrune-VLA's reason, and renames the axis [C78, C12, and the line-281 half of C54]

Quote (relatedwork.tex 281-285): "On the temporal axis, executing one action over several control steps reduces model calls in proportion. But it acts open loop through contacts. FlashVLA gates its action reuse and still reports a success decrease~\cite{flashvla}, and SpecPrune-VLA separates coarse movement from phases that need precision for the same reason~\cite{specprune}."

Evidence: flashvla.txt 556-559 frames the headline 73.7% vs 74.4% as "maintaining the same average success rate" and it is pruning plus reuse; Table 2 lines 666-675 row "w/o Pruned Inference" (reuse alone) is 84.2 then 82.2, 80.6, 81.0, 80.2 on LIBERO-Spatial. SpecPruneVLA.txt 29-33 and 677-680 and 723-727, the coarse/fine split adjusts token pruning aggressiveness because failures cluster in contact phases, and no call is ever skipped. Axis names at lines 259-263 do not include "temporal".

Fix (lines 281-285): "On the first axis, when the policy runs, executing one action over several control steps reduces model calls in proportion. But it acts open loop through contacts. FlashVLA gates its action reuse, and its ablation shows the gated reuse alone lowering LIBERO-Spatial success at every setting~\cite{flashvla}, and SpecPrune-VLA, which prunes tokens rather than calls, keeps more of them in the contact phases where its failures cluster~\cite{specprune}."
Mirror in RelatedWork.md 53-56.

### 3.6 Gromov et al. is presented as a training-free peer of ShortGPT [C0]

Quote (relatedwork.tex 309-310): "while others keep the final layer and remove a contiguous deep block~\cite{gromov}"

Evidence: gromov.txt p.5 lines 295-296 ("drop the deepest layers, excluding the final layer before the LLM head, and then (non-optionally) heal the damage"), p.7 lines 512-514 ("performs quite poorly without healing"). The same paragraph (lines 311-313) excludes MoLe-VLA for needing training.

Fix: "while others keep the final layer, remove a contiguous deep block, and heal the cut by finetuning~\cite{gromov}". Mirror in RelatedWork.md 85-86.

### 3.7 TurboVLA keeps a language model in the action path [C1]

Quote (relatedwork.tex 315): "or dropping the language-model pathway altogether~\cite{turbovla}"

Evidence: TurboVLA.txt 138 ("retains language conditioning while removing the large language model from execution-level control"), 550 (BERT as the instruction encoder), 720 (no language, 70.8% vs 97.7%). The header at relatedwork.tex 186 already has the accurate form "drops the LLM pathway".

Fix: "or dropping the large-language-model pathway altogether~\cite{turbovla}". Mirror in RelatedWork.md 90.

### 3.8 The benchmark sentence counts benchmarks inconsistently, uses a suite name Table I does not, and asserts that LIBERO has four suites [C19, C20, C22, C52]

Quote (relatedwork.tex 360-363): "We evaluate the six families under one protocol on three benchmarks, the WidowX and Fractal suites of SimplerEnv and the four suites of LIBERO, and run each backbone on every benchmark for which a checkpoint at the size we evaluate is released, as listed in Section~IV-A."

Evidence: two SimplerEnv suites count as two benchmarks while four LIBERO suites count as one, and vlaeval.txt Table I lines 196-203 counts each as one benchmark; Table I labels the rows "Bridge" and "Fractal" (tabletricks.tex 29, 71) while the body never uses "Bridge"; Simplerenv.txt 594-597 names the setups "Google Robot" and "WidowX BridgeV2" and never "Fractal"; vlaeval.txt 454-455 ("LIBERO papers report 4 or 5 suites") and 47_Open_VLA_OFT.txt 559 (LIBERO-90). The per-setup unit is needed by the checkpoint rule (MiniVLA and UniVLA have a Bridge checkpoint but no Fractal one).

Fix (lines 360-363): "We evaluate the six families under one protocol on three benchmarks, SimplerEnv WidowX-Bridge, SimplerEnv Fractal and LIBERO with four suites, and run each backbone on every benchmark for which a checkpoint at the size we evaluate is released, as listed in Section~IV-A."
Mirror in RelatedWork.md 137-140. Keep the Table I row labels and have Section IV-A define Bridge as the WidowX BridgeV2 setup, Fractal as the Google Robot setup, and name the four LIBERO suites. "six" is pending item 1 (Section 4.1).

### 3.9 The CNN Bag of Tricks paper is about training refinements, not inference tricks [C23]

Quote (relatedwork.tex 358-360): "Evaluating a set of inference tricks under one protocol, rather than one per paper, is an established practice~\cite{bagoftricks_cnn, bagoftricks_llm}."

Evidence: Bagoftricks_cnn.txt p.1 lines 13-21 ("training procedure refinements"), section headings at lines 131, 216, 377, 603, no occurrence of "inference"; 1651_Bag_of_Tricks_for_Inferen_1.txt lines 12-15 and 29-32 is inference-time computation.

Fix: "Evaluating a set of tricks under one protocol, rather than one per paper, is an established practice~\cite{bagoftricks_cnn, bagoftricks_llm}." Mirror in RelatedWork.md 135-137.

### 3.10 "every step still sees a current image" reads as the policy seeing it [C45, C92]

Quote (bagoftricks.tex 133-135): "Consecutive reuse is capped, and any failed gate returns to dense inference, so every step still sees a current image."

Evidence: tricks/src/vla_tricks/temporal.py 124-131, step() computes the signature from the image and returns _last_action.copy() without calling infer; only the gate reads the frame on a reused step. docs/methodology.md 214 keeps the qualifier "even when model execution is skipped".

Fix: "Consecutive reuse is capped, and any failed gate returns to dense inference, so the gates read a current image at every step and only the model call is skipped."

### 3.11 The fused token is the previous fused token, and nothing is recomputed selectively [C46]

Quote (bagoftricks.tex 144-145): "A reusable patch takes the previous step's projected token, the others are recomputed, and a dense keyframe every few steps bounds propagation."

Evidence: perception.py 315 (self._previous_tokens = fused.detach().clone(), the fused output), 210-214 (fused = current_tokens.clone() then overwrite reusable indices), 303-318 (hook runs on the full projector output every step); docs/methodology.md 313-314 says the same.

Fix: "A reusable patch takes the token that entered the decoder at the previous step in place of the fresh one, the others take the fresh token, and a dense keyframe every few steps bounds propagation."

### 3.12 Two UniVLA candidate rows equal dense because the method never fired [C32, C43]

Quote (tabletricks.tex 33-34): "Guarded reuse moderate & $87.5$ & $190$ & $30.1$" and "Temporal fusion conservative & $87.5$ & $192$ & $30.2$"

Evidence: simpler_widowx/summary.csv line 6 (UniVLA guarded_reuse_moderate avg_reuses 0.005, one reuse in 200 episodes, strict 0), line 7 (conservative_adaptive success 87.5, avg_steps 30.21, avg_policy_calls 6.375, median_reusable_visual_tokens 0.0, identical to the original row); the same backbone's motion_entropy row logs 403.625 tokens. spatialvla_simpler_widowx.csv conservative row also 0.0 (not in Table I).

Fix (append to the footnote string at make_tricks_table.py 173, then regenerate): "On UniVLA, guarded reuse fired on one step in 200 episodes and conservative-adaptive fusion marked no token reusable at the median step, so both rows measure the selector's cost alone." Results should report reuses per episode and median reusable tokens for every row.

---

## 4. Points that must wait on the mentor or on data

### 4.1 Six families versus five (known, pending item 1) [C25, C37, C80]

Quote (relatedwork.tex 251, 266, 360): "the six intervention families we evaluate", "each of the six families below", "We evaluate the six families"; and (295-296) "It is the closest published point of comparison for our candidates, not a contribution of ours."

Evidence: bagoftricks.tex has five subsections (100, 109, 117, 128, 139), Table I has five family rows per backbone, no vla_cache configuration exists under artifacts/results/mentor_csv; figpipeline.tex 48 says six; VLA-Cache's only numbers are per-call GPU time on an RTX 4090 (VLACache.txt 603), which lines 353-356 rule out as a like-for-like comparison.

Fix if no rollout arrives: change "six" to "five" at relatedwork.tex 251, 266, 360, figpipeline.tex 48, RelatedWork.md 19, 38, 137, and change line 295 to "It is the closest published method to our candidates, not a contribution of ours." Do not add "and we do not rerun it".

### 4.2 CronusVLA temporal fusion ran with keyframe interval 1 and its three settings are one run three times (not pending, needs the mentor) [C33, C42, C58, C88]

Quote (tabletricks.tex 62 and 97): "Temporal fusion motion-entropy & $33.5$ & $127$ & $51.2$" and "Temporal fusion motion-entropy & $57.2$ & $157$ & $50.5$"; and (bagoftricks.tex 147) "Three settings are evaluated."

Evidence: cronusvla_simpler_widowx.csv 13-15 and cronusvla_simplerenv_fractal.csv 13-15 all carry fusion_keyframe_interval 1 and fusion_max_reuse_fraction 0.5 (every other backbone's conservative row is 2 and 0.25) with identical successes (67, 143), avg_steps (51.205, 50.536), calls and median_reusable_visual_tokens (110.542, 115.285) across motion-entropy, task-aware and conservative-adaptive. In the exported controller, perception.py 288-290, interval 1 makes every step a keyframe and fuses nothing. The CronusVLA harness is not in the export, so whether its nonzero token count is a selector difference or a logging artifact cannot be settled here. bagoftricks.tex header 50-52 flags it as CONFIRM.

Fix: none in prose. Either rerun CronusVLA fusion at the shared presets (keyframe 3 and fraction 0.5, conservative-adaptive at 2 and 0.25) or add to the Table I footnote "CronusVLA temporal fusion ran with a keyframe every step, so its fusion rows are one configuration and are excluded from the fusion claim."

### 4.3 CronusVLA depth rows violate the stated selector (known, pending item 2) [C63, C93, C96]

Quote (tabletricks.tex 60 and 95): "Depth prune 1 & $35.0$ & $123$ & $52.2$" and "Depth prune 1 & $59.2$ & $150$ & $49.8$"; and (bagoftricks.tex 122-123) "protects the first quarter of the stack and the final block, forbids removing adjacent blocks"

Evidence: csv_digest.md 24-26 and 80-82, removed layers 1; 1,3; 0,2,4,5 on both suites; every other backbone lists deep layers (CogACT 17;23, OpenVLA 23;25, UniVLA 26;30). Layers 0 and 1 sit in the first quarter of any stack and 4,5 are adjacent, so the Table I rows themselves, not only the sweep, contradict the rule.

Fix: none until the mentor reruns or footnotes. The depth rewrite in Section 2.3 is worded so that only the adjacency clause depends on this decision.

### 4.4 "fused attention" is verified for two of six backbones (known, pending item 3) [C38, C51, C64, C86, C94, and the candidate-path half of C50]

Quote (relatedwork.tex 355-356): "by measuring every speedup against fused attention" and (bagoftricks.tex 93-94) "The reference is the released policy run unchanged under fused attention."

Evidence: relatedwork.tex header 139-160, OpenVLA VERIFIED (benchmark_openvla_14.py 159, sdpa), UniVLA VERIFIED (adaptive_sparse_vla/inference.py 791), MiniVLA DEFAULT, SpatialVLA, CogACT, CronusVLA UNKNOWN; those three appear on both benchmarks, so six of ten Table I cells are unverified. tricks/scripts/benchmark_vla_cache.py 130 loads eager, so a VLA-Cache rollout on that script would break the sentence. The task-aware fusion path requests output_attentions=True on every call (benchmark_openvla_14.py 216, 330, 343) and costs +12 to +13 ms per call on OpenVLA and MiniVLA (csv_digest.md 33 vs 45, 103 vs 115, 89 vs 102); docs/methodology.md 256-257 asks that the backend of any attention-map path be disclosed.

Fix: if any backbone ran eager, use the header's fallback in both places, relatedwork.tex 356 "against the dense implementation each backbone ships with" and bagoftricks.tex 94 "released policy run unchanged with the dense implementation it ships with". For the task-aware path, bagoftricks.tex 148-149 can become "task-aware adds text-to-vision relevance read from the preceding model call, which requires the attention weights to be returned and is charged to its latency", and the Setup handoff list should ask Section IV-A to state the attention backend the task-aware path runs under.

### 4.5 The "preregistered" gate (known, pending item 5) [C39, C49, C82]

Quote (relatedwork.tex 364-367): "A candidate is called positive only when it lowers end-to-end latency, with the cost of its own signals included, or raises success, while the other stays within a preregistered margin of dense."

Evidence: no margin value in any experiments/paper file and no Setup section; tricks/docs/experiment_protocol.md 11 has only "a preregistered non-inferiority margin (suggested: -1 percentage point)"; no registration record in the repo or export; git log shows the CSVs landing 2026-09-03 (c9d4f6a, cf84a69, fe2e627) and the gate sentence rewritten 2026-09-04 (ed9976c, header 238-244). The docs' gate differs in content, experiment_protocol.md 5-15 makes speed superiority mandatory, success superior or non-inferior, and adds "no regression on contact-heavy and long-horizon strata"; methodology.md 26-29 and 402-404 and AGENTS.md 5-6 agree. The symmetric form admits a success-only candidate at unchanged latency and invents a latency margin. Borderline rows, CogACT Fractal foveation 67.2 vs 66.8 (tabletricks.tex 71-72), UniVLA 87.5 vs 87.5 (33-34).

Fix: the mentor owns the gate. If the docs' gate stands, lines 364-367 become "A candidate is called positive only when it lowers end-to-end latency, with the cost of its own signals included, and its success is superior to dense or within a non-inferiority margin stated in Section IV-A, with no regression on contact-heavy or long-horizon episodes." If the symmetric form stands, experiment_protocol.md, methodology.md and Setup must be changed to match it and the strata check stated or dropped explicitly. Either way replace "preregistered" with "fixed" and cite the fixing commit in Section IV-A, or write "within a margin of dense stated in Section IV-A". Mirror in RelatedWork.md 141-144.

### 4.6 Paired episodes and the critique of mean-only tables (known, pending item 6) [C36, C83]

Quote (relatedwork.tex 352-356): "The tables we cite report mean success rates, which cannot say on which episodes an intervention helped. A speedup also depends on the dense baseline it is measured against, and an eager attention baseline inflates it relative to a fused one. We remove both, by pairing episodes and by measuring every speedup against fused attention." and (339-340, 363-364) "on paired episodes", "Every comparison is on matched episodes against optimized dense inference."

Evidence: every mentor CSV holds one aggregate row per configuration, Table I (tabletricks.tex 27-97) prints means only with no paired statistic, discordant count or interval; experiment_protocol.md 52 promises "success with Wilson interval and paired McNemar comparison". The harness does pair by seed (run_minivla_simpler_eval.py 315, 332), so "matched episodes" is a true design claim.

Fix: none until per-episode files arrive. If they do not, cut the first sentence at 352-353 and change 355-356 to "We measure every speedup against fused attention." (or the Section 4.4 fallback wording).

### 4.7 One seed and one run per configuration, so no noise floor (not pending, needs data) [C84]

Quote (relatedwork.tex 363-364): "Every comparison is on matched episodes against optimized dense inference."

Evidence: scripts/experiments/original.py 9 and depth_pruning1.py 9 pass --seed 42 once; run_minivla_simpler_eval.py 315, 332 seed each episode as seed plus index; adaptive_sparse_vla sets no seed at all; header line 81 commits Setup to "one run per configuration". No repeated dense run exists in any cell, so a reviewer can ask what a 1 to 2 point difference in Table I means. Note that CronusVLA's three guarded-reuse presets on Bridge give identical rollouts to the episode (72 successes, 49.735 steps, 0.025 reuses, cronusvla_simpler_widowx.csv 10-12), which shows that harness is deterministic under its seed, so a same-seed repeat there would prove nothing.

Fix: none in prose. Ask the mentor for a second dense seed per cell so Setup can state "a second dense seed bounds run-to-run variation at x points".

### 4.8 Seven exact ties in the best-of-family pick with no stated tie rule (not pending, needs the mentor) [the tie half of C28, C97]

Quote (tabletricks.tex 61): "Guarded reuse strict & $36.0$ & $125$ & $49.7$"

Evidence: per-backbone CSVs, UniVLA guarded strict = moderate 87.5 (moderate kept), CronusVLA Bridge guarded strict = moderate = aggressive 36.0 (strict kept, moderate is 124 ms vs 125), UniVLA depth 1 = 2 at 82.0 (2 kept), CogACT Bridge foveation keep 20 = keep 50 at 52.5 (20 kept), CogACT Bridge fusion motion-entropy = task-aware 58.0, CronusVLA fusion three-way ties on both benchmarks. Lower latency and tighter setting both fail as a single rule; fewer mean steps then first-listed fits the picks but is unstated. make_tricks_table.py 9-11 only reads the mentor's pick.

Fix: once the mentor states the rule, add one sentence to the footnote string at make_tricks_table.py 173, for example "Ties in success are broken by fewer mean steps, then by the first-listed setting." Do not write "broken toward the tighter setting", which is false for UniVLA guarded reuse.

### 4.9 The conservative-adaptive forced keyframe is verified for the MiniVLA harness only (known, pending item 8) [C44, C65]

Quote (bagoftricks.tex 150-151): "a forced keyframe whenever any patch moves beyond a threshold"; (tabletricks.tex 34, 76) "Temporal fusion conservative & $87.5$ & $192$ & $30.2$" (UniVLA) and "$65.6$ & $171$ & $44.5$" (CogACT Fractal).

Evidence: run_minivla_simpler_eval.py 381-398 is the only call site passing force_keyframe and that script is MiniVLA only (167-170); benchmark_openvla_14.py 328-345 calls prepare without force_keyframe and line 392 labels it a synthetic latency diagnostic; adaptive_sparse_vla has no keyframe, event or conservative code; no CSV has an event-threshold column. Pending item 8 should read "verified for the MiniVLA harness only".

Fix: none until the mentor confirms the preset on the UniVLA and CogACT harnesses.

### 4.10 Table I carries no printed note on the missing Fractal cells (known, pending item 7, fix ready) [C40]

Quote (relatedwork.tex 362-363): "run each backbone on every benchmark for which a checkpoint at the size we evaluate is released"

Evidence: tabletricks.tex 17 is a %% comment that does not print; the Fractal block (71-97) lists four backbones; google_robot_fractal/ holds four backbone CSVs; the printed footnote says nothing.

Fix: append to the footnote string at make_tricks_table.py 173 "MiniVLA and UniVLA release no Fractal checkpoint and appear under Bridge only." then regenerate.

### 4.11 The Setup handoff list names no hardware, dtype, batch size or step-clock definition [C87]

Quote (bagoftricks.tex 22): "Handoff list of every value that must appear in Setup:"

Evidence: bagoftricks.tex 23-52 holds thresholds and presets only; relatedwork.tex header 81 ("one GPU, one run per configuration") and 122-124 (LIBERO on a different GPU); benchmark_openvla_14.py 159-160 (sdpa, bfloat16, OpenVLA only); no GPU, dtype or backend column in any summary.csv, so the single-GPU claim for Table I cannot be checked from the data. Provisional values exist in the mentor's docs (methodology.md 50-57, literature_review.md 172, RTX 5090).

Fix (append after bagoftricks.tex line 52): "hardware      GPU model and count per backbone (CONFIRM one GPU for all Table I runs, LIBERO ran on a different GPU), dtype BF16, batch size 1, simulator commit and render resolution, and what the step clock avg_episode_time_s / avg_steps spans (CONFIRM, the harness timing code was not read)."

---

## 5. Typos, LaTeX and style

### 5.1 "may" appears twice in the Related Work body (style) [C62, C71]

Quote (relatedwork.tex 307): "the recipes built on it disagree on which layers may go" and (325) "how far it may go".

Evidence: brief.md 115-116 lists "may" among the banned hedges without exception.

Fix: line 307 "the recipes built on it disagree on which layers can go". Line 325 is replaced by the Section 3.2 rewrite. Mirror in RelatedWork.md 83 and 101.

### 5.2 Undefined axis names "spatial axis" and "temporal axis" (style) [C54]

Quote (relatedwork.tex 271, 281): "On the spatial axis, foveation descends from" and "On the temporal axis, executing one action".

Evidence: axes are named at lines 259-263 as "When the policy runs", "What it is shown", "How much network each call uses"; calling the axis that holds temporal fusion the spatial axis reads oddly.

Fix: covered by the rewrites in Sections 1.1 and 3.5 ("On the second axis, what the policy is shown" and "On the first axis, when the policy runs").

### 5.3 Depth wording, layer versus block, Block Influence capitalisation, pruning versus removal (style) [C55, C70, C75]

Quote (bagoftricks.tex 119-120): "Calibrated depth removal scores each decoder block by ShortGPT block influence"

Evidence: relatedwork.tex 306, 308, 316 and figpipeline.tex 27 say "layer" and "Block Influence"; ShortGPT.txt 27 and 121 capitalise the metric; the heading at line 117 is "Depth Pruning". Table I's "Depth prune N" and its footnote already agree with the heading.

Fix: covered by the Section 2.3 rewrite (pruning, layer, Block Influence). Leave Table I and make_tricks_table.py unchanged.

### 5.4 "lengthens by $k$" reads as additive (style) [C73]

Quote (bagoftricks.tex 112-114): "It divides model calls per environment step by $k$ and lengthens by $k$ the interval the robot acts on without fresh feedback, with no gate."

Fix: "It divides model calls per environment step by $k$ and multiplies by $k$ the interval the robot acts on without fresh feedback, with no gate."

### 5.5 "Interaction-aware" collides with VLA-IAP and is used nowhere else (style) [C81]

Quote (bagoftricks.tex 141): "Interaction-aware temporal fusion scores each visual patch by motion, image entropy, and optional text-to-vision relevance"

Evidence: relatedwork.tex 335 cites "VLA-IAP prunes tokens by interaction alignment" two sentences before "Our fusion"; VLAIAP_compressed.txt 65, 130 ("Interaction-First", "Interaction-Aligned Pruning"); the family is "Temporal Fusion" in the heading, Table I and Related Work.

Fix: "Temporal fusion scores each visual patch by motion, image entropy, and optional text-to-vision relevance"

### 5.6 A rule line under a paragraph renders the closing paragraph as a heading (typo) [C74]

Quote (RelatedWork.md 144-145): "the other stays within a preregistered margin of dense." followed directly by "---".

Evidence: CommonMark parses "---" under text as a setext H2 underline; the rules at lines 15 and 17 are set off by blank lines.

Fix: insert a blank line between line 144 and line 145.

### 5.7 Task-aware relevance is one step behind and the first step of each episode has none (style, handoff note) [C98]

Quote (bagoftricks.tex 148-149): "task-aware adds text-to-vision relevance"

Evidence: run_minivla_simpler_eval.py 336 (previous_relevance = None per episode), 397 (task_relevance=previous_relevance passed before the query), 419-422 (overwritten from the query just made); perception.py 151-169 skip the task-protect set when relevance is None. Verified for the MiniVLA harness only; benchmark_openvla_14.py 329-343 uses same-frame relevance in its static diagnostic and adaptive_sparse_vla has no relevance path.

Fix: leave the Methods sentence. Add to the temporal fusion handoff list in the bagoftricks.tex header "task-aware relevance = coarse text-to-vision map recorded from the preceding model call, so the mask at step t uses step t-1 relevance and the first step of each episode has none (run_minivla_simpler_eval.py 336, 397, 419-422). Verified for the MiniVLA harness only. CONFIRM for CogACT, SpatialVLA, CronusVLA, UniVLA."

---

## 6. Bib fixes (experiments/paper/main.bib)

### 6.1 vlapruner points a reviewer at a differently titled paper with two missing coauthors (error) [C8]

Quote (134-141): "title = {{VLA-Pruner}: Temporal-Aware Dual-Level Visual Token Pruning for Efficient Vision-Language-Action Inference}, author = {Liu, Ziyan and Chen, Yeqiu and Cai, Hongyi and Lin, Tao and Yang, Shuo and Liu, Zheng and Zhao, Bo},"

Evidence: pdf_txt/VLAPruner.txt p.1 lines 3-13 ("Bridging the Semantic-Action Gap in Visual Token Pruning for Efficient VLA Inference", nine authors including Yiming Zhang and Runquan Gui), line 54 ("arXiv:2511.16449v5 [cs.CV] 26 May 2026"); an unversioned id resolves to v5. The SIMPLER numbers are identical in v1 and v5 and the paper quotes none, so no number needs rechecking.

Fix:
```
@article{vlapruner,
  title   = {Bridging the Semantic-Action Gap in Visual Token Pruning for Efficient {VLA} Inference},
  author  = {Liu, Ziyan and Chen, Yeqiu and Zhang, Yiming and Cai, Hongyi and Lin, Tao and Gui, Runquan and Yang, Shuo and Liu, Zheng and Zhao, Bo},
  journal = {arXiv preprint arXiv:2511.16449},
  year    = {2025},
}
```
If the 2025 title is kept instead, write arXiv:2511.16449v1 in the journal field.

### 6.2 bagoftricks_llm has no arXiv id and no venue although both are on the PDFs (error) [C24, C69]

Quote (213-214): "journal = {arXiv preprint}, year = {2025},"

Evidence: Bag_of_tricks.txt 96 ("arXiv:2502.07191v4 [cs.AI] 17 Feb 2025"); 1651_Bag_of_Tricks_for_Inferen_1.txt p.1 line 46 ("39th Conference on Neural Information Processing Systems (NeurIPS 2025) Track on Datasets and Benchmarks."); the 2406.09760 in the second file (line 1182) is a reference, not the paper's own id; comment at line 209 and header line 14 flag it as missing.

Fix (replace 209-215, delete the comment, remove bagoftricks_llm from the header list on line 14):
```
@inproceedings{bagoftricks_llm,
  title     = {Bag of Tricks for Inference-time Computation of {LLM} Reasoning},
  author    = {Liu, Fan and Chao, Wen-Shuo and Tan, Naiqiang and Liu, Hao},
  booktitle = {Advances in Neural Information Processing Systems (NeurIPS),
               Datasets and Benchmarks Track},
  year      = {2025},
  note      = {arXiv:2502.07191},
}
```

### 6.3 vlaeval has no arXiv id although it is on the PDF (error) [C18, C68]

Quote (360): "journal = {arXiv preprint},"

Evidence: pdf_txt/vlaeval.txt p.1 line 97 ("arXiv:2603.13966v2 [cs.AI] 17 Apr 2026"); title and authors at lines 3-4 match 356-359.

Fix: line 360 "journal = {arXiv preprint arXiv:2603.13966},", delete the comment on line 354, remove vlaeval from the header list on line 14.

### 6.4 molevla author list disagrees with the PDF title page (error, confirmed, with a caveat from a refuted duplicate) [C67]

Quote (101-103): "Dai, Gaole and Du, Li and Wang, Dan and Du, Yuan and Zhang, Shanghang}"

Evidence: pdf_txt/MoLeVLA.txt p.1 (arXiv:2503.20384v2) lists nine authors with no Dan Wang. Caveat the author must weigh: the refuted duplicate of this finding (appendix, item 2) was rejected because its third verifier fetched the authors' own repository README (RoyZry98/MoLe-VLA-Pytorch, lines 70-77), which publishes a ten-author BibTeX identical to main.bib including "Wang, Dan", and EfficientVLA.txt 1076 cites the same ten-author form. arxiv.org is unreachable from the session.

Fix: keep the entry as it is and add a between-entries note "% molevla: author list follows the authors' own BibTeX (repo README) and the arXiv listing; the v2 title page prints nine authors without Dan Wang." Confirm on the arXiv listing before camera-ready. If the listing shows nine authors, change 102-103 to "and Chi, Xiaowei and Dai, Gaole and Du, Li and Du, Yuan and Zhang, Shanghang},".

### 6.5 vlacache is the paper's main baseline and is cited as a preprint (style) [C5]

Quote (89-96): "@article{vlacache, ... journal = {arXiv preprint arXiv:2502.02175}, year = {2025},"

Evidence: VLACache.txt p.1 line 47 ("39th Conference on Neural Information Processing Systems (NeurIPS 2025)"), line 48 (v2, 21 Oct 2025); vlainfoentropy.txt 794-798 cites it as NeurIPS 2025.

Fix:
```
@inproceedings{vlacache,
  title     = {{VLA-Cache}: Efficient Vision-Language-Action Manipulation via
               Adaptive Token Caching},
  author    = {Xu, Siyu and Wang, Yunke and Xia, Chenghao and Zhu, Dihao and
               Huang, Tao and Xu, Chang},
  booktitle = {Advances in Neural Information Processing Systems (NeurIPS)},
  year      = {2025},
  note      = {arXiv:2502.02175},
}
```

### 6.6 sparsevlm is the ICML 2025 version (style) [C6]

Quote (295-303): "@article{sparsevlm, ... journal = {arXiv preprint arXiv:2410.04417}, year = {2025},"

Evidence: SparseVLM.txt p.1 lines 3, 46-48 ("arXiv:2410.04417v4 [cs.CV] 3 Jun 2025", "Proceedings of the 42 nd International Conference on Machine Learning, Vancouver, Canada. PMLR 267, 2025.").

Fix:
```
@inproceedings{sparsevlm,
  title     = {{SparseVLM}: Visual Token Sparsification for Efficient
               Vision-Language Model Inference},
  author    = {Zhang, Yuan and Fan, Chun-Kai and Ma, Junpeng and Zheng, Wenzhao
               and Huang, Tao and Cheng, Kuan and Gudovskiy, Denis and Okuno,
               Tomoyuki and Nakata, Yohei and Keutzer, Kurt and Zhang, Shanghang},
  booktitle = {International Conference on Machine Learning (ICML)},
  year      = {2025},
  note      = {arXiv:2410.04417},
}
```

### 6.7 flower is a CoRL 2025 paper typed as a preprint (style) [C2]

Quote (230-238): "@article{flower, ... journal = {arXiv preprint arXiv:2509.04996}, year = {2025},"

Evidence: FLOWER.txt p.1 line 50 ("9th Conference on Robot Learning (CoRL 2025), Seoul, Korea."), line 51 (arXiv stamp); the bib's own rule at line 70 ("booktitle only if the acceptance is confirmed"), and act, diffusionpolicy, libero use @inproceedings.

Fix:
```
@inproceedings{flower,
  title     = {{FLOWER}: Democratizing Generalist Robot Policies with Efficient Vision-Language-Action Flow Policies},
  author    = {Reuss, Moritz and Zhou, Hongyi and R{\"u}hle, Marcel and Ya{\u{g}}murlu, {\"O}mer Erdin{\c{c}} and Otto, Fabian and Lioutikov, Rudolf},
  booktitle = {Conference on Robot Learning (CoRL)},
  year      = {2025},
  note      = {arXiv:2509.04996},
}
```

### 6.8 simplerenv is the benchmark the paper runs and has no venue (style) [C21]

Quote (130): "journal = {arXiv preprint arXiv:2405.05941},"

Evidence: vlaeval.txt p.5 lines 547-549 ("in CoRL, 2024"); the second verifier found the paper in the CoRL 2024 proceedings, PMLR volume 270 (proceedings.mlr.press/v270/li25c.html), which meets the bib comment's "Confirm before switching" at line 122. arXiv id and year match Simplerenv.txt p.1 line 85.

Fix: change 123-132 to @inproceedings with "booktitle = {Conference on Robot Learning (CoRL)}, year = {2024}," keeping title and authors, and drop the comment at 120-122.

### 6.9 lookfocusact year does not match its identifier or the supplied PDF (style) [C17]

Quote (260): "% lookfocusact: year is 2026 on purpose (arXiv stamps 2026), not 2025."

Evidence: 56_Look_Focus_Act.txt p.1 line 100 ("arXiv:2507.15833v2 [cs.RO] 22 Sep 2025"), Gazereg.txt 1182-1184 dates it 2025, the authors' GitHub README bibtex gives 2025; a v3 dated Jul 2026 was reported by a search summary but could not be read.

Fix: "year = {2025}," and replace the comment with "% lookfocusact: dated by first arXiv version (Jul 2025); v3 revised Jul 2026."

### 6.10 fastv venue is recorded by two cited papers as ECCV 2024 (style, camera-ready) [C7]

Quote (71-78): "title = {An Image is Worth 1/2 Tokens After Layer 2: Plug-and-Play Inference Acceleration for Large Vision-Language Models}, author = {Chen, Liang and Zhao, Haozhe and Liu, Tianyu and Bai, Shuai and"

Evidence: FastV.txt p.1 lines 4-7 (v3 prints "Plug-and-PLay Acceleration for VLLM Inference" and "Shuang Bai"); VLACache.txt 1057-1060 and vlainfoentropy.txt 886-889 and survey_vla1.txt 1213 cite the ECCV form with "Shuai Bai", pages 19-35 (Springer year 2025 vs 2024 split). The bib note at 67-70 ("nothing in this repository records the venue") is out of date.

Fix: keep title and author list. Replace the note at 67-70 with one recording that VLA-Cache [29] and VLA-InfoEntropy [27] cite the ECCV 2024 proceedings, pp. 19-35. Switch to "@inproceedings{fastv, ..., booktitle = {European Conference on Computer Vision (ECCV)}, year = {2024}, note = {arXiv:2403.06764}}" once the Springer listing confirms the year and the spelling Shuai Bai.

### 6.11 Section header comments no longer describe the entries beneath them (style) [C76]

Quote (17): "% ===================== the interventions we re-measure ====================="

Evidence: line 17 precedes univla, spatialvla, openvlaoft; line 51 ("our backbones and benchmarks") precedes efficientvla, fastv, shortgpt, vlacache, molevla; line 217 ("axis 1 lineage") precedes gazereg and lookfocusact; line 270 ("compact VLAs") precedes schwartz, sparsevlm, specprune, starvla, tome; line 331 ("foveation lineage") precedes turbovla; the file is not alphabetical.

Fix: delete the seven section-header comment lines (17, 51, 171, 217, 270, 331, 352). Do not add an "alphabetical" note.

### 6.12 tome becomes uncited after the Section 1.4 fix [consequence of C3, C79]

Fix: delete the tome entry (323-330) so check_bib.py stays at an exact match, or keep it and accept the mismatch report.

---

## 7. What was checked and found clean (from the auditor notes)

Citation checks
- shortgpt, gromov (cite:depth-llm). Every use located (relatedwork.tex 306-319, bagoftricks.tex 119-126, RelatedWork.md 83-93 match). Methods' Block Influence definition matches ShortGPT eq. (1) p.4 and depth.py 65-68. "constrains nothing further" matches ShortGPT p.4 Sec. 3.2 and Table 10 p.16. Gromov's final-layer exclusion and contiguous block match gromov.txt p.5 and p.7. Protected quarter, final block and no-adjacent rule are stated as ours and match depth.py select_non_adjacent. No numbers quoted from either paper. Bib entries for both match the PDFs (shortgpt arXiv:2403.03853v3, gromov ICLR 2025, arXiv:2403.17887v2). Out of scope note for the mentor: introduction.tex 78-80 and tableI.tex 66 describe Gromov et al. as "deepest layers only", which fits only their simpler heuristic.
- efficientvla, molevla (cite:depth-vla). Only body use at relatedwork.tex 310-313, RelatedWork.md 86-87 match. EfficientVLA is training-free with unconstrained sorted-importance layer removal (EfficientVLA.txt p.1, p.4-5 Sec. 3.2.2, Table 2 p.7, Limitations p.15). MoLe-VLA needs a learnable STAR router with Gumbel-Softmax and self-distillation trained end-to-end (MoLeVLA.txt p.4-7). No numbers quoted. Bib entries match the PDFs apart from the author-list item in 6.4.
- flower, smolvla, turbovla (cite:compact-vla). Single body use at relatedwork.tex 313-317, RelatedWork.md 88-91 match. FLOWER prunes 30-50% of VLM layers (FLOWER.txt p.2-3, p.5), SmolVLA keeps the first 16 layers (SmolVLA.txt p.10, p.13), TurboVLA replaces V to L to A with V + L to A (TurboVLA.txt p.1, p.6); all are training-time designs and the paper does not call them training-free; "compact" is supported by 947M, 450M and 0.2B. No numbers quoted. smolvla and turbovla bib entries match their PDFs (arXiv 2506.01844v1, 2607.27205v2).
- vlacache, vlainfoentropy, fastv, sparsevlm, tome (cite:cache). All body uses at relatedwork.tex 291-303, 333-334, 349, RelatedWork.md matches. VLA-Cache mechanism sentence matches VLACache.txt p.6 Sec. 4.1 and p.13 App. D. Numbers 74.7 vs 75.0 and 31.83 vs 51.91 ms (38.7%) match Table 2 p.8 and the paper's own 1.63x. VLA-Cache is training-free and its backbone grid supports line 349. VLA-InfoEntropy sentence matches its abstract, Fig. 1 caption and conclusion. tome (ICLR 2023) and vlainfoentropy (arXiv:2604.05323v1) bib entries match. tableI.tex 63 VLA-Cache defaults match VLACache.txt p.14.
- vlapruner, vlaiap (cite:pruning). Uses at relatedwork.tex 300-302, 335, 349-350, RelatedWork.md matches. "reproduces the loss on the same OpenVLA setting and attributes it to a prefill versus action-decode attention mismatch" matches VLAPruner.txt p.1 lines 26-29, p.2 108-109, p.4 296-297, Table 1 p.8. VLA-Pruner is training-free. The vlapruner half of line 349-350 holds (OpenVLA on LIBERO and SIMPLER, OFT and pi0 on LIBERO only). "VLA-IAP prunes tokens by interaction alignment" matches VLAIAP_compressed.txt p.1 27-28 and p.25 2036; VLA-IAP is training-free. vlaiap bib entry matches title, ten authors and arXiv:2603.22991v1. No house-style violations in these sentences.
- flashvla, specprune (cite:reuse-gating). Uses at relatedwork.tex 283-285, 322-324, 349. FlashVLA bib matches (eight authors, arXiv:2505.21200v1); SpecPrune-VLA title and seven authors match. "gates reuse on action similarity and visual token stability" is near-verbatim from flashvla.txt p.2. SpecPrune-VLA is visual token pruning conditioned on end-effector velocity, training-free (pp.5-7, Algorithm 1 p.15). SpecPrune-VLA v3 runs OpenVLA-OFT on LIBERO, DB-OFT on WidowX, CogACT on Google-robot, so no backbone crosses benchmarks and line 349 holds (RelatedWork_Sources.md row 10 lists two backbones; v3 has three).
- ttfvla, gazereg, lookfocusact (cite:fusion-gaze). TTF-VLA mechanism clause matches Eqs. 2-4 and "prevent long-term error accumulation" (pp.2-3); four points (68.4 to 72.4, Table 1 p.6) and under two percent overhead (p.5) check; "denoising rather than speed" is fair since TTF-VLA reports no speedup. ttfvla bib (seven authors, AAAI 2026, arXiv 2508.19257) matches. Gaze-Reg is a training-time regularizer with no gaze at inference (Sec. 3.4, Fig. 2, App. E.2), and its crossing-cell claim at line 349-350 holds (pi0 on LIBERO, ALOHA-Sim and real, OpenVLA on LIBERO only). Look-Focus-Act "do shed tokens" (324 to 20 tokens, Table II) and "give up pretrained weights fitted to a uniform grid" (p.5) check; title, authors and id match.
- simplerenv, libero, vlaeval, starvla, effvlasurvey1, effvlasurvey2 (cite:evaluation). All uses at relatedwork.tex 255-258 and 342-362. vla-eval "fourteen benchmarks" and "pitfalls earlier work had left unrecorded" match vlaeval.txt p.1 54, 59-60, p.2 156-159, 280. StarVLA bib matches starvla.txt (arXiv:2604.05014v1, corporate author correctly double-braced); starvlaa.txt is a different paper (StarVLA-alpha, 2604.11757) and is not cited. Both surveys' titles, authors and stamps match (2510.17111v3, 2510.24795v2); survey_vla1 p.1 33-35 supports "inheriting their size and latency". simplerenv title and 16 authors match. libero bib is consistent with vla-eval ref [5] and StarVLA's reference. "The field reports results on SimplerEnv and LIBERO" is supported by vla-eval Table I and starvla.txt 648.
- dqn, act, diffusionpolicy, openvlaoft, schwartz, traver, bagoftricks_cnn, bagoftricks_llm (cite:lineage). DQN uses frame skip with k = 4 and repeats the last action (DQNNaturePaper.txt 610-616); ACT introduces action chunking (ACT.txt 28, 379-421); Diffusion Policy's action execution horizon Ta is executed without re-planning (Diffusion_policy.txt 340-343) and OpenVLA-OFT cites it as prior chunking work; OpenVLA-OFT chunks with K = 8. The Schwartz 1977 scan is the complex logarithmic conformal mapping paper and Traver names it the log-polar model (10rastraver.txt 168-175); "cut data while preserving central resolution" is near-verbatim from Traver 207-221. The LLM Bag of Tricks evaluates six methods across eight tasks under one protocol (1651 file 29-32, 418-419). Bib fields for dqn (Nature 518, 529, 19 authors), act, openvlaoft, bagoftricks_cnn, traver (RAS 58 (2010) 378-398) and schwartz (Biol. Cybernetics 25, 181-194, 1977) match; issue numbers and the CVPR 2019 and RSS venues are not printed on the uploads. RelatedWork.md 31, 45-46, 135-137 match the .tex.

Data, code and consistency checks
- Table I versus the CSVs (lens:csv-vs-text, internal-consistency, reviewer-stats, reviewer-robotics). All 60 rows recomputed from the two summary.csv files match tabletricks.tex exactly (success, avg_episode_time_s x 1000 / avg_steps rounded to ms, avg_steps to one decimal), including labels, backbone order and multirow counts 36 and 24. Each chosen variant is the highest-success variant in its per-backbone CSV. Episode counts are consistent (4 x 50 on Bridge, 5 x 50 on Fractal) across all 14 configurations in all 10 cells. Per-step latency falls under repeat 2 on all ten pairs (27.6 to 44.5%) and under depth pruning on all ten. Header claims verified against the CSVs (UniVLA 4.74 and 9.92 steps per call, guarded reuse 0 to 3.305 reuses per episode, MiniVLA 0.0% at 2 and 4 removed blocks, foveation raising success for SpatialVLA, CogACT and CronusVLA on WidowX, collapses at 4 blocks). Handoff-list preset values match scripts/experiments/*.py and benchmark_openvla_14.py. "Fusion changes which tokens enter the decoder, not how many" is consistent with the projector-hook replacement and the reusable-token medians respecting the caps (108 of 256 under 0.5, 64 under 0.25). The UniVLA depth sets 21, 24, 26, 30 and every other backbone's sets except CronusVLA satisfy the quarter, final-block and non-adjacency rule as written. Foveation latency rises 5 to 20% per step from the CPU blur, consistent with "own signals included" once the footnote names image transforms (Section 2.5). Observation, not a finding: the MiniVLA CSV rows coincide to every decimal with tricks/docs/local_results.md (36.0%, 63.84 steps, 9.542 s), so the local episodes.jsonl is probably the CSV run for that backbone.
- Methods versus the tricks code (lens:code-vs-methods, methods-reproducibility). Blur foveation, action repeat (hold and skip, calls per step 1/k on five backbones), the depth selector on the Prismatic harness (BI, ceil(0.25 N), final block, strict gap, set fixed before the loop, true removal with reindexing), guarded reuse gates and presets (stride-8 signature, frame and 8x8 patch MAE, cosine on dims 0-5 of the two most recent dense actions, gripper sign on dim 6, translation floor 0.01 refusing reuse below it, consecutive cap, fallback; strict 0.01/0.03/0.995/1, moderate 0.015/0.04/0.99/1, aggressive 0.02/0.05/0.98/2), the fusion selector (motion, entropy, optional relevance, 3x3 dilation, floor(fraction x 256) budget, keyframe by step modulo, unchanged token count) and all handoff values match temporal.py 96-142, perception.py 166-191, 210-214, 288, 303-318 and the experiment scripts. Every Table I label has a Methods definition. "Related Work gives the lineage of each" holds for all five families. Sticky gripper exists only in inference.py's google_robot and widowx branches, applied equally to dense, and no paper sentence touches it. Unverifiable from the export: the UniVLA harness contains no foveation, repeat, guarded reuse or fusion path, the OpenVLA, CogACT, SpatialVLA and CronusVLA rollout harnesses are absent, and MiniVLAPolicy.predict is not in the export. Pending item 4 (calibration split) was not raised.
- Mentor docs versus the text (lens:mentor-docs-vs-text). Control, baseline and candidate roles match the docs' table; first-party wording is limited to the three candidates and no mechanism novelty is claimed; the anonymous OpenReview submission is not cited and the shared cache mask is absent from both bodies; VLA-Cache numbers agree with the docs; guarded reuse, fusion, action repeat and depth descriptions match methodology.md sections 6, 7, 9; the docs' eager diagnostic, combined system, oracle ablations and phase-stratified rates are absent but not claimed.
- Internal consistency (lens:internal-consistency, typos-latex-style). check_bib.py reports 36 entries and 36 cited keys, exact match; check_reading_copies.py reports the reading copy matches. RelatedWork.md body 19-144 matches relatedwork.tex 249-367 sentence by sentence (1231 words). The five methods named as ours in Related Work are the five Methods subsections, each on one axis. No em-dashes, semicolons or prose colons in either body; hedges limited to the two "may"; American spelling throughout; every \cite preceded by ~; \% escaped; \textbf and \emph balanced; math symbols in math mode; "Section~IV-A" used identically; table column spec, \cmidrule and \multirow counts correct; caption precedes label. Dangling references resolve. Method names match the PDFs (FlashVLA mixed case is defensible). main.bib parses with balanced braces, required fields present, Last, First author form, StarVLA corporate author double-braced; author lists and titles verified against the PDFs for 24 entries. No .bst present, so title lowercasing ("Lego-like") could not be tested. audit_sections.py reports flashvla, ttfvla and vlainfoentropy unmapped only because its NAME table was not extended.
- Reviewer lenses (novelty, stats, efficiency, robotics). Every competitor sentence checked against the PDFs is accurate apart from the items above. The Methods sections state no numbers and make no per-episode or interval claim of their own. "Every encoder we run splits the image into a uniform grid" holds for the Prismatic DINOv2+SigLIP backbones, SigLIP in SpatialVLA and Emu3's VQ tokenizer in UniVLA (SpatialVLA's Ego3D depth path could not be checked, no PDF). "Queries the policy on none of the repeated steps" and "divides model calls per environment step by $k$" check numerically. Guarded reuse's signal cost is inside the clock and where reuse is rare the gate costs more than it saves (CogACT Fractal aggressive 176 vs 169 ms), which the gate is meant to expose. "acts open loop through contacts" is a mechanism statement not contradicted by the Fractal numbers. The MiniVLA harness calibrates on seed + 10000 + index (a disjoint seed on the same tasks), noted only, per pending item 4.

---

## Appendix. Findings considered and refuted (not in the report body)

1. relatedwork.tex 307-309 "But the recipes built on it disagree on which layers may go. ShortGPT ranks every layer by Block Influence, the criterion we adopt, and constrains nothing further" (ShortGPT and Gromov said to agree on the final layer; refuted, the sentence is about the recipes' constraints).
2. main.bib 101-103 "author = {Zhang, Rongyu and Dong, Menghang and ... Wang, Dan and Du, Yuan and Zhang, Shanghang}," (Dan Wang absent from the PDF; refuted because the authors' own README BibTeX carries the ten-author list; see 6.4 for the confirmed duplicate and the reconciled action).
3. main.bib 311-312 "journal = {arXiv preprint arXiv:2509.05614}, year = {2026}," (specprune year; documented deliberate choice, consistent across the file).
4. main.bib 240-248 "booktitle = {Proc. IEEE/CVF Conf. Computer Vision and Pattern Recognition Workshops (CVPRW)}, pages = {11332--11342}," (gazereg venue; confirmed correct by the CVF open-access record).
5. relatedwork.tex 346-347 "StarVLA describes the field as fragmented across incompatible codebases" (accurate subset of the source's list).
6. main.bib 220-228 diffusionpolicy seven-author RSS 2023 entry with note arXiv:2303.04137 (correct conference citation).
7. tabletricks.tex 69 "Temporal fusion motion-entropy & $39.0$ & $150$ & $61.2$" (MiniVLA row's empty keyframe cells are a logging gap; preset confirmed from the launcher and local_results.md).
8. relatedwork.tex header 214-215 "repeat 2 harms none of four backbones on Fractal" (comment only, and the proposed fix was false).
9. relatedwork.tex 317-318 "We add protected regions and a ban on adjacent removals to the ShortGPT selector" (redundant with Section 2.3).
10. bagoftricks.tex 135-137 "The image and agreement thresholds and the cap are swept together as one setting, strict, moderate, or aggressive, from the tightest to the loosest, with the translation floor fixed." (CronusVLA Bridge identical presets are a Section IV data note, not a Section III error; see 4.7 and 4.8).
11. relatedwork.tex 305-306 "Three families act only where a signal says it is safe." (Block Influence is the depth signal).
12. RelatedWork.md 152-154 "VLA-Cache moved ahead of the candidates because the temporal-fusion cache path is VLA-Cache-compatible ..." (dated changelog entry superseded by later notes).
13. relatedwork.tex 282-283 "But it acts open loop through contacts." (predicative use, no hyphen needed).
14. main.bib 74 "author = {Chen, Liang and Zhao, Haozhe and Liu, Tianyu and Bai, Shuai and" (spelling follows arXiv metadata and the author's other papers; see 6.10).
15. RelatedWork.md 48, 53 "(Look Focus Act)" (citation label from audit_sections.py, not a title rendering).
16. relatedwork.tex 353-355 "A speedup also depends on the dense baseline it is measured against, and an eager attention baseline inflates it relative to a fused one." (holds for end-to-end step time with a fixed environment cost).
17. relatedwork.tex 264-266 "A claim about efficiency is a claim about one of these resources being spent differently, so we treat them as axes rather than competing methods, and each of the six families below acts on one of them." (temporal fusion acts on axis 2 as written).
18. relatedwork.tex 356-357 "We exclude quantization, which lowers numerical precision rather than any of the three resources above" (consistent with the paper's own axis definitions).
19. bagoftricks.tex 125-126 "The budget, how many blocks are removed, is the swept quantity." (grammatical appositive, taste).
20. bagoftricks.tex 92 "maps an image and an instruction to an action $a_t$" ($a_t$ anchors the Setup handoff's cos(a_{t-2}, a_{t-1})).

---

## Summary counts (merged items)

Errors 8 (1.1, 1.2, 1.3, 1.4, 1.6, 2.2, 2.7, 2.8). Contradictions 8 (1.5, 1.7, 2.1, 2.3, 2.4, 2.5, 2.6, 2.9). Reviewer-attackable, prose-fixable 12 (3.1 to 3.12). Waiting on the mentor or data 11 (4.1 to 4.11, of which seven are known pending items 1, 2, 3, 5, 6, 7, 8). Typos and style 7 (5.1 to 5.7). Bib 12 (6.1 to 6.12). Refuted and listed for reference 20.
