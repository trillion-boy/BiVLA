% ---------------------------------------------------------------------------
% III. Bag of Tricks -- the methods section, created 2026-08-26 to match the
% lab-meeting outline (III. Bag of Tricks {A. Preliminaries, B. Methods}).
%
% A. Preliminaries: the inference loop and the notation the rest of the paper
%    uses (chunk, repeat, horizon). It does NOT re-explain the three axes --
%    Related Work already defines them, and repeating them here is the echo
%    Reviewer 2 flagged in Setup. Preliminaries only fixes notation.
% B. Methods: the three interventions as implemented. This paragraph was in
%    setup.tex (draft 2); it moved here so Setup carries only the protocol.
%
% Style matches the rest: no em-dashes, no semicolons, no prose colons.
% Specs (checkpoint, depth, chunk, GPU) live in Table II, not in prose.
% ---------------------------------------------------------------------------

\section{Bag of Tricks}

\subsection{Preliminaries}

A vision-language model (VLM) maps an image and a text prompt to text through a
visual encoder and a transformer decoder over the resulting tokens. A
vision-language-action (VLA) policy adapts a pretrained VLM to control. It keeps
the encoder and decoder and attaches an action head that reads robot actions off
the decoder's output, fine-tuned on demonstrations~\cite{openvla, spatialvla,
univla}. It therefore inherits the VLM's parameter count and per-call latency,
which is the cost the interventions in this paper act on.

Concretely, a VLA policy maps an observation to actions in three stages. An
encoder turns the image and the instruction into tokens, a decoder of
transformer layers processes them, and the action head reads out one or more
actions. The policy is queried during a rollout, and each query returns a
\emph{chunk} of $c$ actions that are sent to the environment in order. A
backbone's chunk length is a property of its checkpoint and is one, one, and
five for the three we run (Table~\ref{tab:backbones}).

Two settings govern how often the decoder runs relative to the environment. The
chunk length $c$ fixes how many environment steps one query already covers, and
an \emph{action repeat} $r$ holds each returned action for $r$ consecutive
steps. One query therefore advances the environment by $c\,r$ steps, a quantity
we call the \emph{horizon}. The horizon, not $r$ alone, is what a temporal
intervention changes, and because $c$ differs across backbones the same $r$
gives a different horizon on each.

Each of the three interventions below spends one resource: how often the
decoder is queried, what the encoder is shown, or how many decoder layers each
query runs. We apply every intervention to the released checkpoint with no
retraining.

\subsection{Methods}

\emph{Action repetition} holds every returned action for $r$ consecutive
environment steps, so a two-action chunk at $r{=}2$ executes as
$a_1a_1a_2a_2$, and we sweep $r \in \{2, 4\}$. It changes the horizon and
leaves the cost of a single query untouched.

\emph{Visual foveation} applies a log-polar warp to the observation and inverts
it before the image reaches the encoder, at keep fractions we sweep, in two
variants, log-polar and blur. The warp is a pixel-space edit that runs outside
the network.

\emph{Depth pruning} scores each decoder layer by its input-output redundancy,
the Block Influence criterion, and removes the $k$ most redundant layers within
an eligibility window over the stack. We hold the number of removed layers
fixed rather than the fraction, since the stacks are 32, 32, and 26 layers deep
(Table~\ref{tab:backbones}), and sweep $k \in \{1, 2, 4\}$.
