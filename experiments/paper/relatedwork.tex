% ---------------------------------------------------------------------------
% II. Related Work -- SIX-FAMILY version, v3, 2026-09-03. The canonical file.
% The three-method version is kept as relatedwork_3method_backup.tex.
%
% Sources: docs/literature_review.md, docs/methodology.md and
% docs/experiment_protocol.md on the tricks branch, plus the provenance notes
% in RelatedWork_Sources.md for every claim carried over from v1.
%
% Structure, in the order Methods (III.B) and Results walk through:
%   1. Inference cost in VLA policies      the three resource axes
%   2. Generic shortcuts as controls       foveation, unconditional repeat
%   3. The recent baseline                 VLA-Cache, and why VLM token
%                                          methods transfer poorly
%   4. Conditional candidates              depth, guarded reuse,
%                                          temporal fusion + shared mask
%   5. How these claims are evaluated      benchmarks, the missing
%                                          comparison, our protocol
% Each paragraph ends on our position, the bag-of-tricks house style.
%
% Length: this is the LONG draft (about 900 words of prose). Final target is
% 0.75 page, roughly 800 words in ieeeconf. Polish down from here; the cut
% candidates are marked in RelatedWork.md.
%
% Citations: all keys used below are in main.bib. flashvla, ttfvla and
% vlainfoentropy were added 2026-09-03 from the PDFs, not from abstracts.
% The anonymous OpenReview submission R6d86jMO74 that docs/literature_review.md
% raises is deliberately NOT cited. It is non-archival and under review with no
% public version, so a reviewer cannot check it. Its point, that a reuse mask is
% not novel on its own, is carried by ttfvla and vlainfoentropy instead, and the
% concurrent-work clause stays in prose with no reference. See
% RelatedWork_Sources.md row 31.
%
% Style: no em-dashes, no semicolons, no prose colons, no hedges.
% ---------------------------------------------------------------------------

\section{Related Work}

We cover the cost of VLA inference, the six intervention families we evaluate,
grouped by the role each plays in our argument, and how the literature tests
such claims.

\textbf{Inference cost in VLA policies.} VLA policies adapt pretrained
vision-language models to output robot actions, inheriting their size and
latency, and the resulting efficiency literature already has surveys of its
own~\cite{effvlasurvey1, effvlasurvey2}. We divide that literature by the
resource each method spends. 1)~\emph{When the policy runs}. One decision is
executed over several control steps, a lineage running from frame skip in
reinforcement learning to action chunking~\cite{dqn, act, diffusionpolicy}.
2)~\emph{What it is shown}. Visual tokens are reduced, reweighted, or reused.
3)~\emph{How much network each call uses}. Only part of the decoder runs. A
claim about efficiency is a claim about one of these resources being spent
differently, so we treat them as axes rather than competing methods, and each
of the six families below acts on one of them.

\textbf{Generic shortcuts, used as negative controls.} The simplest way to
spend less is to degrade the input or to skip feedback unconditionally. Both
fail in ways that motivate the safeguards the candidates carry. On the spatial
axis, foveation descends from Schwartz's log-polar mapping~\cite{schwartz},
taken up in robot vision to cut data while preserving central
resolution~\cite{traver} and applied to VLAs by gaze-conditioned
policies~\cite{gazereg, lookfocusact}. Every encoder we run splits the image
into a uniform grid, so at a fixed output resolution a pixel-space edit leaves
the visual token count and the model computation unchanged. Foveation before
the encoder therefore tests whether the policy survives losing its periphery,
not whether it runs faster. Methods that foveate inside the encoder do shed
tokens, but they give up pretrained weights fitted to a uniform
grid~\cite{lookfocusact}. On the temporal axis, executing one action over
several control steps reduces model calls in proportion. But it acts open loop
through contacts. FlashVLA reports a 0.7-point average success decrease with
training-free action reuse~\cite{flashvla}, and SpecPrune-VLA separates coarse
movement from precision-sensitive phases for the same reason~\cite{specprune}.
We run fixed foveation and fixed repeat as controls that establish why the
candidate methods need gates.

\textbf{The recent baseline.} VLA-Cache reuses the key-value computation of
visually static patches while recomputing dynamic and task-relevant ones, the
latter selected by language-model attention, so history is retained unlike
under token dropping~\cite{vlacache}. On base OpenVLA across LIBERO it reports
average success of 74.7\% against 75.0\% dense at 31.8~ms against 51.9~ms, a
speed result with nearly maintained rather than improved success. That makes
it the anchor against which we measure candidates, not a first-party
contribution. It also explains a pattern the token-pruning literature
reports. Methods developed for vision-language models~\cite{fastv, sparsevlm,
tome} transfer poorly to VLAs, which VLA-Cache attributes to VLAs' short
action sequences~\cite{vlacache} and VLA-Pruner reproduces on another
setting~\cite{vlapruner}. Whatever is removed from the visual stream must be
removed by a signal that knows what the robot is doing.

\textbf{Conditional candidates.} Three families remove computation only where
a signal says it is safe. \emph{Depth.} Decoder-layer redundancy is well
established in language models. But the recipes built on it disagree on which
layers may go. ShortGPT ranks every layer by Block Influence, the criterion we
adopt, and constrains nothing further~\cite{shortgpt}, while others keep the
final layer and remove a contiguous deep block~\cite{gromov}. EfficientVLA
carries the unconstrained form into VLAs without training~\cite{efficientvla},
and MoLe-VLA needs a learned router and distillation~\cite{molevla}, which
places it outside a training-free study. Several recent compact VLAs build
such reductions in by design rather than applying them at
inference~\cite{flower, smolvla, turbovla}, so whether removing layers helps
depends on what the architecture already leaves out. We add protected regions
and a non-adjacency rule to the ShortGPT score and calibrate on a disjoint
split. \emph{Guarded reuse.} Where fixed repeat skips feedback blindly,
guarded reuse skips a model call only when the current image and the recent
trajectory are both stable. Recent work ties reuse to action similarity and
visual-token stability~\cite{flashvla} or to the manipulation
phase~\cite{specprune} on the same reasoning. \emph{Temporal fusion with a shared cache mask.} TTF-VLA fuses
visual tokens across frames without training and raises base OpenVLA on LIBERO
by four points at under two percent overhead, a denoising result rather than a
speed one~\cite{ttfvla}. VLA-InfoEntropy selects which tokens to reuse by
image and attention entropy~\cite{vlainfoentropy}, and VLA-IAP prunes tokens by interaction
alignment~\cite{vlaiap}. Selecting patches for reuse is therefore not itself
new, and concurrent work is converging on coordinated reuse across modalities.
What we test is one mask that drives both denoising and cache reuse, with
contact-aware fallbacks, against optimized dense inference on paired episodes.

\textbf{How these claims are evaluated.} The field reports results on
SimplerEnv~\cite{simplerenv} and LIBERO~\cite{libero}, and recent work
addresses the infrastructure around them. The vla-eval harness unifies
fourteen benchmarks and documents evaluation pitfalls earlier work had left
unrecorded~\cite{vlaeval}, and StarVLA describes the field as fragmented
across incompatible codebases~\cite{starvla}. But infrastructure cannot supply
the comparison itself. Papers using several backbones change the benchmark at
the same time~\cite{vlacache, specprune, vlaiap}. Those with both factors
present leave the crossing cell empty~\cite{gazereg, vlapruner}. The tables we
cite report mean success rates rather than matched-episode outcomes, and a
speedup measured against eager attention rather than fused attention
overstates the gain. We exclude quantisation, which changes none of the three
resources, and learned early exit, which needs training. Isolating the effect
of choices that appear only in source code is an established
practice~\cite{bagoftricks_cnn, bagoftricks_llm}. We evaluate the six
families under one protocol, on matched episodes against optimized dense
inference, so that a candidate is called positive only when its speed and its
success both clear a preregistered gate.
