% ---------------------------------------------------------------------------
% II. Related Work -- SIX-FAMILY version, v3, 2026-09-03. The canonical file.
% The three-method version is kept as relatedwork_3method_backup.tex.
%
% Sources: docs/literature_review.md, docs/methodology.md and
% docs/experiment_protocol.md on the tricks branch, plus the provenance notes
% in RelatedWork_Sources.md for every claim carried over from v1.
%
% Structure, in the order Methods (III.B) and Results walk through:
%   1. Inference cost in VLA policies      the three resource axes
%   2. Generic shortcuts as controls       foveation, unconditional repeat
%   3. The recent baseline                 VLA-Cache, and why VLM token
%                                          methods transfer poorly
%   4. Conditional candidates              depth, guarded reuse,
%                                          temporal fusion + shared mask
%   5. How these claims are evaluated      benchmarks, the missing
%                                          comparison, our protocol
% Each paragraph ends on our position, the bag-of-tricks house style.
%
% Length: this is the LONG draft (about 1450 words of prose). Final target is
% 0.75 page, roughly 800 words in ieeeconf. Polish down from here; the cut
% candidates are marked in RelatedWork.md.
%
% Citations: all keys used below are in main.bib. flashvla, ttfvla and
% vlainfoentropy were added 2026-09-03 from the PDFs, not from abstracts.
% The anonymous OpenReview submission R6d86jMO74 that docs/literature_review.md
% raises is deliberately NOT cited. It is non-archival and under review with
% no public version, so a reviewer cannot check it. Its weak point, that a
% reuse mask is not novel on its own, is carried by ttfvla and vlainfoentropy
% instead. Its strong point, that someone already shares one mask across both
% paths, has no citable source and is not stated. See RelatedWork_Sources.md
% row 31.
%
% Ownership: three of the six are first-party recipes (calibrated depth,
% guarded reuse, shared-mask fusion), per docs/literature_review.md line 22.
% The text says "we add", "our guarded reuse", "we build" for those, and
% "baseline" / "control" for the other three. Novelty of the mechanisms is
% not claimed anywhere; the contribution is the protocol.
%
% Data-driven edits, 2026-09-03, after the mentor's 12 CSVs arrived:
%   "why the candidate methods need gates" -> "when". Fixed repeat 2 collapses
%   four of six backbones on WidowX but harms none of four on Fractal, so the
%   gate is needed on contact tasks, not always. Results carries the split.
%   "across six backbones and two benchmarks" added to the last sentence,
%   because the paragraph faults others for the empty crossing cell and four
%   of our backbones fill it.
%   The compact-VLA sentence (FLOWER, SmolVLA, TurboVLA) is no longer a cut
%   candidate: MiniVLA, CronusVLA and SpatialVLA collapse under depth pruning.
% Pending the mentor: "calibrate on a disjoint split" (line ~108). The harness
%   calibrates on the same tasks and seed as the test run, so confirm before
%   camera-ready. Also pending: the VLA-Cache paragraph, if no rollout is run.
%
% Review-response edits, 2026-09-03, after a simulated review:
%   (1) "Selecting patches by a computed signal is therefore not new" removed.
%       The design is now stated positively (one mask, cache reuses only what
%       fusion treats as stable). No "first" claim: literature_review.md bars
%       it, and no collapse-when-combined claim: there is no such experiment.
%   (2) Guarded reuse now names its differences from FlashVLA: pixel gates
%       before any network call, whole-frame and local scale, two-action
%       agreement in direction and gripper, one reused step. FlashVLA's gate
%       is token-aware, so it is NOT described as "broad" or "global".
%   (3) "leave the crossing cell empty" -> "do not report"; "overstates the
%       gain" -> an eager attention baseline inflates a speedup relative to
%       a fused one, and we fix the baseline. States a measurement fact, no
%       verdict on any paper and no claim about which backend they used.
%   (4) "six backbones and two benchmarks" STAYS. The review assumed LIBERO
%       and one backbone from experiment_protocol.md, but setup.tex and the
%       mentor's CSVs are SimplerEnv WidowX + Fractal. The real inconsistency
%       is that setup.tex still describes the three-backbone grid; it is
%       rewritten with Results once the per-episode files arrive.
%       UPDATE 2026-09-03: the mentor writes Setup and Protocol. What RW
%       commits Setup to, so the two sections agree:
%         (list refreshed 2026-09-04, eleventh pass, to match the body)
%         - six intervention families, two controls, one baseline, three
%           candidates (five in Methods and Table I; VLA-Cache rollout
%           pending)
%         - three evaluation settings, WidowX Bridge, Google Robot (Fractal),
%           LIBERO with four suites; every backbone on every setting with a
%           released checkpoint at the evaluated size; counts not hardcoded
%         - matched episodes paired by seed; paired test once the
%           per-episode files arrive
%         - dense baseline under fused attention (SDPA / FlashAttention),
%           backend per backbone to be confirmed
%         - a candidate is positive when it lowers end-to-end latency with
%           its own signal cost included, or raises success, while the other
%           stays within a preregistered margin of dense
%         - depth calibration on a disjoint split (confirm; the harness
%           calibrates on the test tasks and seed)
%         - one GPU, one run per configuration
%
% Second review round, 2026-09-03:
%   (1) FlashVLA's 0.7-point figure removed. It is the decrease of FlashVLA's
%       GATED reuse, so it now reads "gates its action reuse and still reports
%       a success decrease". Unconditional repeat failing is our own result.
%   (2) Closing sentence says "six backbones, four of them on both
%       benchmarks": the grid is 10 of 12 cells (no Fractal checkpoint for
%       MiniVLA, UniVLA). If Results fills fewer cells, drop the
%       crossing-cell clause.
%   (3) Depth: "choose the removed set on a disjoint split with no weight
%       update". Calibration picks layers, not thresholds.
%   (4) Gate is on end-to-end latency with the candidate's own signal cost
%       included, per experiment_protocol.md line 9.
%
% Third review round, 2026-09-03:
%   "We hold both fixed" -> "We remove both, by pairing episodes and by
%       measuring every speedup against fused attention". "both" was reading
%       as the nouns before it.
%   Quantization "changes none of the three resources" -> "lowers numerical
%       precision rather than any of the three quantities above".
%   "four of them on both benchmarks" -> "crossing the four that release
%       checkpoints for both benchmarks with both". Same count, states the
%       reason.
%   VLA-Cache "nearly maintained rather than improved success" -> "at nearly
%       unchanged success". The contrast with TTF-VLA's denoising result is
%       still carried by "a speed result" here and "a denoising result" there.
%
% Fourth pass, 2026-09-03: SpecPrune-VLA is token pruning (title: Action-Aware
%   Self-Speculative Pruning), so "gates reuse ... or on the manipulation
%   phase" misattributed reuse to it. Now "conditions its token pruning on the
%   manipulation phase".
%
% Grid sentence, 2026-09-03: the paper now has Table I (SimplerEnv WidowX +
%   Fractal) and Table II (LIBERO, four suites), with SmolVLA as a seventh
%   backbone and OpenVLA/LIBERO still running. Counts are therefore NOT
%   hardcoded any more. The closing sentence names the three benchmarks and
%   states the rule: every backbone runs on each benchmark with a released
%   checkpoint. Put numbers back only when the grid is frozen.
%
% Fifth pass (reviewer self-check), 2026-09-04:
%   VLA-Cache latency now a ratio (39% lower), not 31.8 vs 51.9 ms. Table II
%     will show our own OpenVLA dense latency on LIBERO on a different GPU,
%     and absolute ms invite a comparison the hardware does not support.
%   "removed from the visual stream" -> "dropped from the token stream".
%     Foveation edits pixels and drops no tokens, and Table I shows it
%     helping SpatialVLA, so the rule must be about tokens.
%   Grid sentence: "a checkpoint at the size we evaluate". SpatialVLA README
%     (fetched 2026-09-04): four checkpoints, none for LIBERO. CronusVLA
%     README (fetched 2026-09-04, AFTER this note was first written on
%     memory): LIBERO checkpoints exist at 7B only, the single 0.5B
%     checkpoint is Bridge + RT-1. Both support the sentence.
%   Temporal fusion, 2026-09-04: heading and closing sentence no longer
%     mention the shared cache mask. See bagoftricks.tex header.
%   STILL OPEN, cannot be fixed in prose: "six intervention families" vs
%     five in Methods and the tables (VLA-Cache rollout pending); "Both
%     fail" vs repeat 2 on Fractal (Results must state the exception);
%     "preregistered".
%   "measuring every speedup against fused attention", checked per backbone
%     in the tricks-branch code on 2026-09-04:
%       OpenVLA     MICROBENCH ONLY (twelfth pass): scripts/benchmark_openvla_14.py:159
%                             attn_implementation="sdpa", logged as
%                             "attention_backend": "sdpa"
%       UniVLA      VERIFIED  adaptive_sparse_vla/inference.py:791
%                             Emu3MoE.from_pretrained(..., attn_implementation
%                             ="sdpa")
%       MiniVLA     DEFAULT   loaded through openvla-mini's prismatic
%                             base_llm.py (fetched from GitHub): inference
%                             mode builds the LLM from config with no
%                             attention argument, so it takes the
%                             transformers default (SDPA for Qwen2 in current
%                             versions). Fused, but not pinned.
%       SpatialVLA  UNKNOWN   no load call in the tricks-branch harness; the
%                             vendored modeling_gemma2.py supports sdpa,
%                             flash_attention_2 and eager, choice not visible
%       CogACT      UNKNOWN   no source tree on the tricks branch
%       CronusVLA   UNKNOWN   no source tree on the tricks branch
%     Ask the mentor for SpatialVLA, CogACT, CronusVLA. If any ran eager,
%     change to "against the dense implementation each backbone ships with"
%     and drop "fused".
%
% Verification status of the citations, 2026-09-04. The uploads folder holds
% the PDFs from 2026-08-21 onward, so every citation that carries a concrete
% claim has now been read from its PDF: shortgpt, gromov, smolvla, turbovla,
% flower (README), vlaeval, gazereg, ttfvla, flashvla, vlainfoentropy,
% vlacache, vlapruner, specprune, vlaiap, efficientvla, molevla,
% lookfocusact, traver, starvla. Quotes are in RelatedWork_Sources.md rows
% 27 to 46. Also read from PDF (row 46): the two surveys, dqn (frame skip
% quote), openvlaoft (action chunking), the two bag-of-tricks papers, and
% the titles of fastv, sparsevlm, tome. NOT read, no PDF available, cited
% for existence only: act, diffusionpolicy, schwartz, simplerenv, libero.
% That is 28 of 33 keys read; the 5 unread carry no mechanism or number.
% Eighth pass, 2026-09-04, from the VLA-Cache and VLA-Pruner PDFs:
%   "so history is retained unlike under token dropping" removed. Not in
%     VLA-Cache. (CORRECTION, tenth pass: the SHORT-OUTPUT reason IS in
%     VLA-Cache p. 8; only the "history retained" gloss was the mentor's.)
%   Poor transfer of VLM pruning: VLA-Cache attributes it to ignoring the
%     temporal structure of closed-loop control, not to "short action
%     sequences". VLA-Pruner reproduces it on the SAME OpenVLA / LIBERO
%     setting and attributes it to a prefill versus action-decode attention
%     mismatch. Both now stated as the papers state them.
%   Crossing cell: VLA-Pruner does run OpenVLA on both LIBERO and SIMPLER
%     (OFT and pi0 on LIBERO only), so "do not report the crossing cell" was
%     wrong for it. Now "where one backbone does appear on two benchmarks
%     the others appear on one" (vlapruner, gazereg), which both satisfy.
% Seventh pass, 2026-09-04, from those PDFs:
%   TurboVLA does not remove layers, it drops the LLM pathway (direct
%     V + L -> A, 0.2B). The compact-VLA sentence now splits FLOWER/SmolVLA
%     (keep part of the layers) from TurboVLA (no language-model pathway).
%   Gaze-Reg is a training-time regularizer with no gaze at inference, so
%     "policies conditioned on gaze" was wrong. Now "as a training-time
%     regularizer (gazereg) or as a foveation signal (lookfocusact)".
%   Gaze-Reg crossing cell confirmed: pi0 on LIBERO + ALOHA-Sim, OpenVLA on
%     LIBERO only. ShortGPT BI formula and "no further constraint"
%     confirmed. SmolVLA "first 16 layers" confirmed. vla-eval "14
%     simulation benchmarks" and "previously undocumented pitfalls"
%     confirmed.
%
% Sixth pass, 2026-09-04, after reading the TTF-VLA PDF (local upload):
%   TTF-VLA's mechanism is a per-patch hard fusion mask from grayscale pixel
%     difference plus attention relevance, previous token reused elsewhere,
%     keyframe anchoring (its Sec. 3.2 and abstract). With the cache path
%     gone, our temporal fusion is that recipe with a different selector.
%     The paragraph now says so and names only what we change: entropy
%     term, one-patch protective ring, reuse-fraction cap, six backbones.
%   "the anchor against which we measure candidates" -> "the closest
%     published point of comparison". No VLA-Cache rollout exists, so the
%     old sentence was false today regardless of the rollout decision.
%   openvlaoft added to the chunking lineage in axis 1. It was cited in the
%     Introduction only, and the axis had no VLA citation.
%
% Ninth pass (adversarial logic read, both sections), 2026-09-04:
%   "Both fail in ways that motivate the safeguards" -> "Both remove detail or
%     feedback without a signal, which is the case the candidates' safeguards
%     exist to avoid". The CSVs contradict "both fail": repeat 2 harms none of
%     four backbones on Fractal, foveation helps SpatialVLA, CogACT and
%     CronusVLA on WidowX. The controls are defined by what they lack, not by
%     a promised failure.
%   Controls sentence now says what each is a control FOR: same axis as one
%     candidate, no signal (foveation for temporal fusion, fixed repeat for
%     guarded reuse). Answers "why these two".
%   "Three families remove computation" -> "act". III.E says temporal fusion
%     changes which tokens enter the decoder, not how many.
%   "permits one reused step" -> "caps consecutive reused steps". The
%     aggressive preset caps at 2 (scripts/benchmark_openvla_14.py GUARDED
%     table, scripts/experiments/guarded_reuse_aggressive.py:
%     --reuse-max-consecutive 2). One was true of strict and moderate only.
%   "keeps TTF-VLA's hard fusion and keyframe and changes the selection" ->
%     "shares ... and differs in the selection". "keeps" read as built on
%     their code.
%   "six backbones" in the fusion paragraph -> "every backbone in our grid".
%     Table II adds SmolVLA, so the count is no longer hardcoded anywhere.
%   "three quantities above" -> "three resources above", the word axis 1-3
%     use.
%   Bag-of-tricks sentence: "Isolating the effect of choices that appear only
%     in source code" described the cited papers' objects, not ours (our
%     tricks are published methods). Now "Evaluating a set of inference
%     tricks under one protocol, rather than one per paper".
%   Closing gate: "latency and success both clear a gate" made temporal
%     fusion, an accuracy candidate by III.E, unable to be positive by
%     construction. Now "lowers latency, with its own signal cost included,
%     or raises success, while the other stays within a preregistered margin
%     of dense". This is the symmetric form of experiment_protocol.md gates 1
%     and 2 (speed superiority; success superiority or a non-inferiority
%     margin). SETUP MUST STATE THE SAME GATE. Flag for the mentor.
% Tenth pass, 2026-09-04, from the 88-agent audit (scratchpad audit_report.md,
% every item below survived two adversarial verifiers and a spot-check of the
% PDF text by hand):
%   Look, Focus, Act is a ViT policy with gaze, not a VLA (no "language" in
%     its body), so "brought to VLAs through human gaze ... or as a foveation
%     signal" was wrong. Now "brought to learned manipulation policies through
%     gaze, as a training-time attention regularizer in a VLA (gazereg) or as
%     the center of a foveated tokenizer in a ViT policy (lookfocusact)".
%   "spatial axis" / "temporal axis" were never defined. Now "the second
%     axis, what the policy is shown" and "the first axis, when the policy
%     runs".
%   FlashVLA's headline 73.7 vs 74.4 is pruning + reuse and the paper frames
%     it as "maintaining the same average success rate". The reuse-alone
%     ablation ("w/o Pruned Inference", Table 2, LIBERO-Spatial) is 84.2 dense
%     then 82.2, 80.6, 81.0, 80.2 across the token budgets. Now "its ablation
%     shows the gated reuse alone lowering success at every token budget".
%   SpecPrune-VLA prunes tokens by end-effector speed and never skips a call;
%     "separates coarse movement from phases that need precision for the same
%     reason" misattributed. Now "which prunes tokens rather than calls,
%     keeps more of them in the contact phases where its failures cluster"
%     (its Sec. 6.1).
%   Controls sentence: the ninth-pass pairing "foveation for temporal fusion"
%     had no source and foveation is not fusion minus a signal. Now "Fixed
%     repeat is guarded reuse with its gate removed, and foveation degrades
%     the input with no signal at all, so the pair establishes what the
%     candidates' signals buy". "negative controls" -> "controls" in the
%     heading and in III (repeat 2 clears the gate on Fractal).
%   VLA-Cache: "base OpenVLA" was wrong, it uses the official LIBERO
%     finetuned weights (its Sec. 5.1); TTF-VLA likewise uses the
%     task-finetuned LIBERO checkpoints and reserves "base OpenVLA-7B" for
%     SimplerEnv. Both fixed. "39% lower latency" is CUDA latency per call,
%     now "GPU latency per call" so it is not read against Table I's step
%     clock.
%   VLA-Cache compared FastV and SparseVLM only (its Sec. 5.1: "two
%     state-of-the-art token-level acceleration techniques"), ToMe appears in
%     its related work only. tome dropped from the cite and from main.bib.
%     Its stated reasons (its p. 8): "operate within a single frame and
%     disrupt spatial fidelity" and "target long output sequences, whereas
%     VLA models generate short action outputs (e.g., 7 tokens)". The
%     eighth-pass note that the short-output reason was "the mentor's gloss"
%     was WRONG; it is in the paper. "ignoring the temporal structure of
%     closed-loop control" was our paraphrase and is gone.
%   VLA-IAP runs OpenVLA-OFT, DreamVLA, pi0 and pi0.5 on LIBERO, VLABench and
%     CALVIN, with three backbones sharing LIBERO, so it belongs to the
%     "one backbone on two benchmarks, the others on one" group, not to
%     "several backbones also change the benchmark". Moved.
%   Gromov et al.: the deep-block heuristic is defined with mandatory
%     finetuning ("(non-optionally) heal the damage") and "performs quite
%     poorly without healing". Added "and heal the cut by finetuning" so the
%     paragraph's exclusion of MoLe-VLA for needing training is consistent.
%   TurboVLA keeps BERT as a text encoder in the action path (ablation:
%     no language 70.8% vs 97.7%). "dropping the language-model pathway
%     altogether" -> "dropping the large language model from the action path
%     altogether".
%   Guarded reuse vs FlashVLA: FlashVLA also uses the previous two actions,
%     decides before the call, and caps consecutive reuse at one (its
%     Algorithm 1: "if Reuse Action is True and last reuse is False"). Two of
%     our three "differences" were shared. Now the difference is the signal
%     (its token-set comparison from previous calls vs our current-frame
%     pixels, plus gripper check and translation floor), and the shared
%     parts are stated as shared.
%   Fusion vs TTF-VLA: TTF-VLA's mask is pixel difference OR attention
%     relevance (its Eq. 4), so "differs in the selection" overstated. Now
%     "shares ... two signals, pixel motion and text-to-vision attention, the
%     second in one setting only. It adds an image-entropy term, protects a
%     ring ..., and caps the reusable fraction". "one-patch" (a value) gone.
%   Bag of Tricks for CNNs is about training refinements, so "inference
%     tricks" -> "tricks".
%   Benchmark sentence counted two SimplerEnv suites as two benchmarks and
%     four LIBERO suites as one, and Table I says "Bridge". Now "the WidowX
%     Bridge and Fractal setups of SimplerEnv and LIBERO with its four
%     suites".
%   "may go" (hedge) -> "can go".
%   STILL OPEN after this pass, see bagoftricks.tex header: depth selector
%     and removal mechanism differ between the two harnesses; CronusVLA
%     fusion ran with keyframe interval 1 (three identical rows); one seed,
%     no noise floor; tie rule in the best-of-family pick; hardware/dtype.
% Eleventh pass, 2026-09-04, from the 31-agent recheck of the tenth-pass
% sentences (all confirmed by two verifiers, spot-checked by hand):
%   Gaze-Reg was filed under the foveation lineage ("foveation ... brought
%     to learned manipulation policies through gaze, as a training-time
%     attention regularizer"). Gaze-Reg positions itself AGAINST foveated
%     imagery (its p. 2) and its only foveated variant loses 7 points (its
%     App. D.2). Now a separate sentence: "Gaze also enters a VLA with the
%     input left unchanged, as a training-time attention regularizer".
%   FlashVLA ablation: the 256-token column equals dense (84.2), the other
%     four are lower, and the table is LIBERO-Spatial only. Now "its
%     LIBERO-Spatial ablation ... at every reduced token budget".
%   VLA-Cache gives its two reasons for two outcomes: single-frame pruning
%     for the SUCCESS loss, short outputs for the MISSING SPEEDUP. Split
%     accordingly. "GPU latency per call" -> "CUDA latency" (the paper never
%     defines the span of one measurement).
%   Fusion cap: TTF-VLA fixes a fusion rate (30%/50%) in its VLA-Cache
%     setting, so the cap is not purely ours. Stated.
%   Controls: "foveation ... establishes what the candidates' signals buy"
%     had no candidate to point at. Now fixed repeat pairs with the reuse
%     gates and foveation tests perceptual simplification alone
%     (methodology.md sec. 4). "its gate" -> "every gate", "either gate" ->
%     "any gate" (III.D lists more than two).
%   Guarded reuse in RW said "recent trajectory ... stable"; the translation
%     floor refuses reuse when the arm is nearly still. Now "the recent
%     actions agree while the arm is still moving".
%   "Papers that use several backbones also change the benchmark": VLA-Cache
%     runs OpenVLA and OFT both on LIBERO. Now "run at least one of them on a
%     benchmark the others do not see". VLA-Pruner calls its real robot a
%     benchmark, so "two simulation benchmarks".
%   "Fractal" is not a SimplerEnv name (the paper says Google Robot and
%     WidowX BridgeV2). Now "Google Robot (Fractal)". "three benchmarks"
%     counted 2 + 1 in different units; now "three evaluation settings".
%   Opening "The simplest way to spend less is to degrade the input" claimed
%     a saving foveation does not deliver. Now "The simplest shortcuts
%     degrade the input or skip feedback unconditionally."
%   Header "What RW commits Setup to" list refreshed (was the 09-03 list).
% Twelfth pass, 2026-09-05, from the final 48-finder audit (one agent per
% cited paper, a claim ledger per paragraph, three reviewer personas); 67
% findings survived two verifiers, the rest I checked by hand:
%   "VLA policies adapt pretrained VLMs" -> "Most VLA policies" (TurboVLA,
%     cited below, does not). "A claim about efficiency is a claim about one
%     of these resources" -> "Each efficiency claim we consider" (the
%     paragraph itself excludes quantization as spending none of them).
%   Axis 2 gloss did not cover foveation ("Visual tokens are reduced,
%     reweighted, or reused"; foveation edits pixels and "reweighted" had no
%     referent). Now "The image is degraded before the encoder, or its visual
%     tokens are reduced or reused". Axis 3 "How much network each call uses"
%     overlapped token pruning; now "How much of the decoder each call runs".
%   dqn was inside the action-chunking cite; now cited for frame skip alone
%     ("the frame skip of Atari agents (dqn)", the Nature paper credits the
%     technique to earlier Atari work).
%   "control steps" -> "environment steps" (the unit Methods and Table I
%     use); "reduces model calls in proportion" -> "per step" (per episode
%     it does not: UniVLA calls rise 6.4 -> 7.25 under repeat 2).
%   Foveation lineage: the blur that ran is not log-polar, so "descends
%     from Schwartz's log-polar mapping" -> "keeps resolution at the center
%     and sheds it outward, a pattern Schwartz modelled as a log-polar
%     mapping".
%   "fixed repeat" (RW only) -> "action repeat" everywhere; "with every gate
%     removed" -> "with every gate removed and the cap alone setting the
%     interval" (the cap is not a gate).
%   "where its failures cluster" -> "where failures under pruning cluster"
%     (SpecPrune's observation is about aggressive pruning, not its method).
%   VLA-Cache "closest published point of comparison for our candidates":
%     each candidate has a closer relative named two paragraphs later
%     (EfficientVLA, FlashVLA, TTF-VLA). Now "the recent published baseline
%     for training-free VLA acceleration".
%   "where a VLA emits" -> ", whereas a VLA emits" (source contrast).
%   "Whatever is dropped ... must be chosen by a signal" overclaimed (FastV
%     at 50 tokens matches dense in VLA-Cache's own Table 4). Now "Both
%     accounts fault a selection signal that ignores what the robot is
%     doing."
%   "Three families act only where a signal says it is safe": depth is
%     static at test time. Added "once at calibration for depth and at
%     every step for the other two".
%   Gromov: "keep the final layer, remove a contiguous deep block" was their
%     ablation heuristic; the principal algorithm picks the block by
%     input-output similarity anywhere. Now "remove one contiguous block
%     chosen by input-output similarity and heal the cut by finetuning".
%   EfficientVLA names its cut "non-contiguous"; stated so a reviewer cannot
%     quote the heading against "unconstrained". MoLe-VLA: "needs a learned
%     router and distillation" -> "trains a layer router end to end with
%     self-distillation" (its Table 5 ablates the distillation).
%   Guarded reuse vs FlashVLA: "before any model call" is shared (FlashVLA
%     decides before the call too); the difference is that FlashVLA's gate
%     never sees frame s. "inferred actions" was wrong for FlashVLA (its
%     memory can hold a reused copy). "frame and patch scale" -> "whole-frame
%     and local scale" ("patch" means token elsewhere). "while the arm is
%     still moving" -> "still command translation" (the gate reads the
%     command, not the arm). SpecPrune's signal named (end-effector speed
%     from emitted actions, which our translation floor also reads) so
%     "Ours differs in the signal" is scoped to FlashVLA.
%   TTF-VLA: "raises ... checkpoints by four points" -> "reports that this
%     raises average success on ..." (attributed, like VLA-Cache's number).
%     "the second in one setting only" -> "uses the second in our task-aware
%     setting only" (TTF-VLA uses attention in every OpenVLA setting). Cap:
%     "which TTF-VLA fixes outright in its VLA-Cache setting and leaves to
%     the mask on OpenVLA" (its p. 5: fusion rates 30%/50% for VLA-Cache,
%     none for OpenVLA).
%   "every backbone in our grid" -> "every backbone we evaluate" ("grid" is
%     the image grid two sentences earlier). "optimized dense inference" ->
%     "dense inference under fused attention" (twice; "optimized" was never
%     defined).
%   Crossing-cell sentence: VLA-Cache runs OpenVLA and OFT both on LIBERO,
%     so "the effect of a backbone cannot be separated" was refutable from
%     its Table 2. Now "Among the papers we cite ... so no cited grid shows
%     whether an intervention's response to a change of benchmark holds
%     across backbones".
%   "learned early exit, which needs training" now cites DeeR-VLA (NeurIPS
%     2024, "we design a tailored training method"); effvlasurvey2 files
%     DeeR-VLA under training-free, so the citation forestalls that.
%   "three evaluation settings" -> "three evaluation environments"
%     ("setting" is a preset in III and Table I).
%   NEW mentor questions (bagoftricks.tex header): OpenVLA's attention
%     backend is verified only in the microbenchmark, not the rollout
%     harness; the guarded reuse, fusion, foveation and repeat code for
%     CogACT, SpatialVLA, CronusVLA and UniVLA is not on the tricks branch;
%     CronusVLA Bridge guarded reuse presets gave three identical rollouts;
%     CronusVLA's step clock equals its policy time (no simulator time?).
% Thirteenth pass, 2026-09-05, independent recheck of the twelfth-pass
% sentences (5 finders, 5 verifiers, plain-text returns):
%   "the recent published baseline" claimed uniqueness; EfficientVLA, FlashVLA,
%     SpecPrune and VLA-Pruner are also recent training-free accelerators.
%     Now "the training-free VLA acceleration method that later work adopts
%     as its baseline (vlapruner, efficientvla, specprune)", which is what
%     their baseline tables show.
%   Gromov: healing is "(Optionally)" in the principal algorithm; now
%     "offer finetuning as an optional step to heal the cut".
%   "Both accounts fault a selection signal that ignores what the robot is
%     doing": VLA-Cache's account never mentions the robot. Now "one for
%     reading a single frame and one for reading the prefill alone".
%   "EfficientVLA carries the unconstrained ranking" implied it took
%     ShortGPT's criterion over; it never cites ShortGPT. Now "applies the
%     same unconstrained ranking".
%   "ours the two most recent dense ones": FlashVLA's Algorithm 1 updates
%     its memory only on inference, so its two actions are inferred too.
%     Clause dropped.
%   "Among the papers we cite" -> "that test an intervention" (vla-eval's
%     reproduction matrix crosses backbones and benchmarks).
%   "a learned manipulation policy ... in a ViT policy" -> "robot learning
%     ... in a ViT policy"; "modelled" -> "modeled".
%   Refuted by the verifier and left as is: the ACT cite (chunking is its
%     definition), "contact phases" (SpecPrune's own words), delta-z sign
%     (its abstract omits it too), the action-repeat identity (definitional).
%
% Style: no em-dashes, no semicolons, no prose colons, no hedges.
% ---------------------------------------------------------------------------

\section{Related Work}

We cover the cost of VLA inference, the six intervention families we evaluate,
grouped by the role each plays in our argument, and how the literature tests
such claims.

\textbf{Inference cost in VLA policies.} Most VLA policies adapt pretrained
vision-language models to output robot actions, inheriting their size and
latency, and the resulting efficiency literature already has surveys of its
own~\cite{effvlasurvey1, effvlasurvey2}. We divide that literature by the
resource each method spends. 1)~\emph{When the policy runs}. One decision is
executed over several environment steps, a lineage running from the frame skip
of Atari agents~\cite{dqn} to action chunking~\cite{act, diffusionpolicy,
openvlaoft}. 2)~\emph{What it is shown}. The image is degraded before the
encoder, or its visual tokens are reduced or reused. 3)~\emph{How much of the
decoder each call runs}. Layers are removed. Each efficiency claim we consider
is a claim about one of these resources being spent differently, so we treat
them as axes rather than competing methods, and each of the six families below
acts on one of them.

\textbf{Generic shortcuts, used as controls.} The simplest shortcuts degrade
the input or skip feedback unconditionally. Both remove detail or feedback
without a signal, which is the case the candidates' safeguards exist to avoid.
On the second axis, what the policy is shown, foveation keeps resolution at the
center and sheds it outward, a pattern Schwartz modeled as a log-polar
mapping~\cite{schwartz}, taken up in robot vision to cut data while preserving
central resolution~\cite{traver} and brought into robot learning as a
gaze-centered foveated tokenizer in a ViT policy~\cite{lookfocusact}. Gaze also
enters a VLA with the input left unchanged, as a training-time attention
regularizer~\cite{gazereg}. Every encoder we run splits the image into a
uniform grid, so at a fixed output resolution an edit in pixel space leaves the
visual token count and the model computation unchanged. Foveation before the
encoder therefore tests whether the policy survives losing its periphery, not
whether it runs faster. Methods that foveate inside the encoder do shed tokens,
but they give up pretrained weights fitted to a uniform
grid~\cite{lookfocusact}. On the first axis, when the policy runs, executing
one action over several environment steps reduces model calls per step in
proportion. But it acts open loop through contacts. FlashVLA gates its action
reuse, and its LIBERO-Spatial ablation shows the gated reuse alone lowering
success at every reduced token budget~\cite{flashvla}, and SpecPrune-VLA, which
prunes tokens rather than calls, keeps more of them in the contact phases where
failures under pruning cluster~\cite{specprune}. We run fixed foveation and
action repeat as controls. Action repeat is guarded reuse with every gate
removed and the cap alone setting the interval, and that pair establishes what
the reuse gates buy. Foveation degrades the input with no signal, and
establishes whether perceptual simplification alone costs success.

\textbf{The recent baseline.} VLA-Cache reuses the cached key-value entries of
visually static patches and recomputes those that move or that the decoder's
attention marks task-relevant~\cite{vlacache}. On OpenVLA's LIBERO checkpoints
it reports average success of 74.7\% against 75.0\% dense at 39\% lower CUDA
latency, a speed result at nearly unchanged success. It is the training-free
VLA acceleration method that later work adopts as its baseline~\cite{vlapruner,
efficientvla, specprune}, not a contribution of ours. The same paper also
reports that two token pruning methods developed for vision-language
models~\cite{fastv, sparsevlm} transfer poorly to VLAs. It attributes the
success loss to their working within a single frame, which disrupts spatial
fidelity, and the missing speedup to their targeting long output sequences,
whereas a VLA emits a few action tokens~\cite{vlacache}. VLA-Pruner reproduces
the loss on the same OpenVLA setting and attributes it instead to a mismatch
between the attention patterns of the prefill and the action-decode
stage~\cite{vlapruner}. Both accounts fault the selection signal, one for
reading a single frame and one for reading the prefill alone.

\textbf{Conditional candidates.} Three families act only where a signal says it
is safe, once at calibration for depth and at every step for the other two.
\emph{Depth.} Redundancy among decoder layers is well established in language
models. But the recipes built on it disagree on which layers can go. ShortGPT
ranks every layer by Block Influence, the criterion we adopt, and constrains
nothing further~\cite{shortgpt}, while others remove one contiguous block
chosen by the similarity between its input and output and offer finetuning as
an optional step to heal the cut~\cite{gromov}. EfficientVLA applies the same
unconstrained ranking to a VLA without training, under the name non-contiguous
pruning~\cite{efficientvla}, and MoLe-VLA trains a layer router end to end with
self-distillation~\cite{molevla}, which places it outside a training-free
study. Several recent compact VLAs build the reduction in by design, keeping
only part of the language model's layers~\cite{flower, smolvla} or dropping the
large language model from the action path altogether~\cite{turbovla}, so
whether removing layers at inference helps depends on what the architecture
already leaves out. We add protected regions and a ban on adjacent removals to
the ShortGPT selector, and choose the removed set on a disjoint split with no
weight update. \emph{Guarded reuse.} Where action repeat skips feedback
blindly, our guarded reuse skips a model call only when the current image is
stable and the recent actions agree and still command translation, and falls
back to dense inference the moment any gate fails. Recent work gates reuse on
action similarity and visual token stability~\cite{flashvla}, and SpecPrune-VLA
sets its pruning budget by the end-effector speed read from the actions it has
already emitted~\cite{specprune}, the translational part of which our floor
also reads. Against FlashVLA, ours differs in the signal. FlashVLA compares the
visual token sets its previous two calls selected, whereas ours reads
subsampled pixels of the current frame, which FlashVLA's gate never sees, at
whole-frame and local scale, and adds a gripper-state check and a translation
floor. Both read the angle between their two preceding actions and both cap
consecutive reuse. \emph{Temporal fusion.} TTF-VLA fuses visual tokens across
frames without training. It keeps the current token for patches flagged by
grayscale pixel difference or attention relevance, reuses the previous token
elsewhere, and anchors a keyframe to bound drift. It reports that this raises
average success on OpenVLA's task-finetuned LIBERO checkpoints by four points
at under two percent overhead, a denoising result rather than a speed
one~\cite{ttfvla}. VLA-InfoEntropy selects tokens for VLA-Cache's key-value
reuse by image and attention entropy~\cite{vlainfoentropy}, and VLA-IAP prunes
tokens by interaction alignment~\cite{vlaiap}. Our fusion shares TTF-VLA's hard
fusion, keyframe and two signals, pixel motion and text-to-vision attention,
and uses the second in our task-aware setting only. It adds an image-entropy
term and a protective ring around every flagged patch, and caps the reusable
fraction, which TTF-VLA fixes outright in its VLA-Cache setting and leaves to
the mask on OpenVLA. We test it on every backbone we evaluate against dense
inference under fused attention on paired episodes.

\textbf{How these claims are evaluated.} The field reports results on
SimplerEnv~\cite{simplerenv} and LIBERO~\cite{libero}, and recent work
addresses the infrastructure around them. The vla-eval harness unifies fourteen
benchmarks and documents evaluation pitfalls earlier work had left
unrecorded~\cite{vlaeval}, and StarVLA describes the field as fragmented across
incompatible codebases~\cite{starvla}. But infrastructure cannot supply the
comparison itself. Among the papers we cite that test an intervention, those
that use several backbones run at least one of them on a benchmark the others
do not see~\cite{vlacache, specprune}, and where one backbone does appear on
two simulation benchmarks the others appear on one~\cite{vlapruner, gazereg,
vlaiap}, so no cited grid shows whether an intervention's response to a change
of benchmark holds across backbones. The tables we cite report mean success
rates, which cannot say on which episodes an intervention helped. A speedup
also depends on the dense baseline it is measured against, and an eager
attention baseline inflates it relative to a fused one. We remove both, by
pairing episodes and by measuring every speedup against fused attention. We
exclude quantization, which lowers numerical precision rather than any of the
three resources above, and learned early exit, which trains the exits into the
policy~\cite{deervla}. Evaluating a set of tricks under one protocol, rather
than one per paper, is an established practice~\cite{bagoftricks_cnn,
bagoftricks_llm}. We evaluate the six families under one protocol on three
evaluation environments, the WidowX Bridge and Google Robot (Fractal)
environments of SimplerEnv and LIBERO with its four suites, and run each
backbone on every environment for which a checkpoint at the size we evaluate is
released, as listed in Section~IV-A. Every comparison is on matched episodes
against dense inference under fused attention. A candidate is called positive
only when it lowers end-to-end latency, with the cost of its own signals
included, or raises success, while the other stays within a preregistered
margin of dense.