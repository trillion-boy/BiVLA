% Overview figure (Fig. 1). Rendered by experiments/paper/make_overview.py
% (candidate A3) into figures/overview_A3.pdf at 505.89 pt, the \textwidth of
% ieeeconf letterpaper, so width=\textwidth places it unscaled.
% Fonts 7 pt body, 6.5 pt sub-text, 8 pt card titles, all Liberation Sans
% including the math. Copy figures/overview_A3.pdf to Overleaf next to main.tex.
\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/overview_A3.pdf}
  \caption{Where each intervention enters the control loop of a frozen VLA
  policy. Candidates (top) act on the model call, the visual tokens, and the
  decoder depth. Controls (bottom) act on the input image and on how long
  each action is held. Dots and the diamond mark the point of effect and
  the dashed path the skipped call. Every intervention is compared with the
  dense reference on matched episodes.}
  \label{fig:overview}
\end{figure*}
