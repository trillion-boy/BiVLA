[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "vla-tricks"
version = "0.1.0"
description = "Training-free OpenVLA inference interventions and audits"
requires-python = ">=3.10,<3.11"

[tool.setuptools.packages.find]
where = ["src"]

