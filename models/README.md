# Models

Checkpoint files are intentionally not included in the shareable bundle.
They can be several gigabytes each and may have separate access terms.

Run `./setup_models.sh` from this directory to download OpenVLA 7B from
Hugging Face. Set `HF_TOKEN` when the selected repository requires access.
Other checkpoints can be supplied with `MODEL_ID` and `MODEL_DIR`.
