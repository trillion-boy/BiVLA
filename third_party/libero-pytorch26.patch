diff --git a/libero/libero/benchmark/__init__.py b/libero/libero/benchmark/__init__.py
--- a/libero/libero/benchmark/__init__.py
+++ b/libero/libero/benchmark/__init__.py
@@ -161,7 +161,10 @@ class Benchmark(abc.ABC):
             self.tasks[i].problem_folder,
             self.tasks[i].init_states_file,
         )
-        init_states = torch.load(init_states_path)
+        # LIBERO init files contain NumPy arrays; keep compatibility with
+        # PyTorch >=2.6's weights_only default.
+        init_states = torch.load(init_states_path, weights_only=False)
         return init_states

     def set_task_embs(self, task_embs):
diff --git a/libero/lifelong/evaluate.py b/libero/lifelong/evaluate.py
--- a/libero/lifelong/evaluate.py
+++ b/libero/lifelong/evaluate.py
@@ -251,7 +251,7 @@ def main():
         init_states_path = os.path.join(
             cfg.init_states_folder, task.problem_folder, task.init_states_file
         )
-        init_states = torch.load(init_states_path)
+        init_states = torch.load(init_states_path, weights_only=False)
         indices = np.arange(env_num) % init_states.shape[0]
         init_states_ = init_states[indices]

diff --git a/libero/lifelong/metric.py b/libero/lifelong/metric.py
--- a/libero/lifelong/metric.py
+++ b/libero/lifelong/metric.py
@@ -104,7 +104,7 @@ def evaluate_one_task_success(
         init_states_path = os.path.join(
             cfg.init_states_folder, task.problem_folder, task.init_states_file
         )
-        init_states = torch.load(init_states_path)
+        init_states = torch.load(init_states_path, weights_only=False)
         num_success = 0
         for i in range(eval_loop_num):
             env.reset()
