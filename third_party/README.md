# Third-party dependencies

The dependency checkouts are intentionally omitted from this bundle. Run
`setup_third_party.sh` to clone the audited revisions used by the project.
The script also applies the three documented PyTorch 2.6 compatibility edits
to LIBERO.
